[env]
platform = espressif32
board = esp32-c3-devkitm-1
build_flags =
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=1
framework = arduino
lib_extra_dirs = ../lib
lib_deps =
    adafruit/Adafruit BME680 Library
    adafruit/Adafruit Unified Sensor
    knolleary/PubSubClient
    bblanchon/ArduinoJson
    jandrassy/TelnetStream

[env:usb]
monitor_speed = 115200
monitor_rts = 0
monitor_dtr = 0

[env:ota]
upload_protocol = espota
; OTA host is supplied at flash time by flash.js (--name -> <name>.local), e.g.
;   npm run hw:flash:environment-sensor -- --transport ota --name office_env_sensor --define DEVICE_NAME=office_env_sensor --define ROOM_NAME=office
monitor_rts = 0
monitor_dtr = 0
