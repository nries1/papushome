// Copy this file to config.h and fill in your values.
// config.h is gitignored and will never be committed.

// Network
const char* WIFI_SSID = "YOUR_WIFI_SSID";
const char* WIFI_PASS = "YOUR_WIFI_PASSWORD";
const char* MQTT_HOST = "YOUR_SERVER_IP";         // e.g. "192.168.1.100"
const char* API_HOST  = "http://YOUR_SERVER_IP";  // e.g. "http://192.168.1.100"
const char* DISPLAY_TOKEN = "YOUR_DISPLAY_TOKEN"; // generate: openssl rand -hex 24

// Time — adjust offset for your timezone
const char* ntpServer = "pool.ntp.org";
const long gmtOffset_sec = -18000;    // EST (UTC-5). Change for your timezone.
const int daylightOffset_sec = 3600;  // 3600 if Daylight Saving applies, else 0
