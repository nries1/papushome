# Background
I just added redwoodjs to this repo because I want to offload the crud app portion of the app onto redwoodJS' react, graphql, and prisma stack. The problem is that redwood needs yarn, so I want to migrate my existing services to use yarn instead of npm. The pre-redwood services are in the server/ directory, robot-vision-publisher, and robot-vision-worker. And are all managed with docker-compose.yml

# Implementation
Do not commit anything. Just let me know when the diff is ready and I will commit it.

## Phase 1
Database migration

### Diff 1
1. Spin up the old database (pre-redwood) and obtain the existing database schema by execing into the docker container for the old db `docker exec -it papu-db-1 psql -U user -d plants`
2. Add all of the existing database schema into api/db/schema.prisma
3. Spin up the psql database in this repo with docker
3. Migrate prisma
3. Seed the new prisma db with all of the data from the old db

## Phase 2
Migrate the old api, contained in the server/api directory into redwood's graphql framework

### Diff 1 (in progress)

Migrating `server/database/dao.ts` functions one domain at a time into `api/src/graphql/*.sdl.ts` + `api/src/services/*/`.

**Done:**

- **Devices** — `devices.sdl.ts`, `api/src/services/devices/{get,create,update,delete}.ts`
  - `schema.prisma`: `friendlyName`, `hardwareVersion`, `metadata` changed from nullable to required (`hardware_version` backfilled to `'v1'` for the 4 pre-existing rows that had `NULL`)
  - `devices(page, rows, order)` — offset pagination, replaces old `getDevices`
  - `Device.room` relation resolver (lazy, per-row)
- **Rooms** — `rooms.sdl.ts`, `api/src/services/rooms/get.ts`
  - `Room.devices` relation resolver replaces the old `getRoomsWithDevices` raw-SQL `json_agg` join — client just requests `rooms { devices { ... } }` instead
  - `getRoomIdForDevice` needed no dedicated resolver — it's just `device.room.id` via the existing relation
- **Watering events** — `wateringEvents.sdl.ts`, `api/src/services/wateringEvents/{get,create,update}.ts`
  - `wateringEvents(deviceId, page, rows, order)` consolidates old `getWaterHistory` (device filter) and `getRecentWateringEvents` (no filter) into one query — `deviceId` is optional
  - `createWateringEvent` replaces `appendWaterHistory` (status still hardcoded server-side to `'requested'`)
  - `updateWateringEvent` replaces `updateWaterEvent`
  - `latestWateringEvents` replaces `getLatestWaterEventPerDevice` — used Prisma `distinct: ['deviceId']` + matching `orderBy` instead of raw `DISTINCT ON` SQL
  - `WateringEvent.device` relation resolver matches by the `deviceId` string (no formal FK exists between `watering_events` and `devices` in the schema)
  - Not yet smoke-tested end to end: `gallonsUsed` is a Prisma `Decimal` mapped to GraphQL `Float` — should serialize fine via decimal.js's `valueOf()`, but unverified at runtime

- **Tank readings** — `tankReadings.sdl.ts`, `api/src/services/tankReadings/{get,create}.ts`
  - `tankReadings(deviceId, rows)` replaces `getWaterLevels` — no `page`/`order` args since the old function only ever took a flat `rows` limit, newest-first
  - `createTankReading` replaces `appendWaterLevel` — takes `pctFull` directly (`Int!`) instead of the old `percent_full` float that got `Math.round()`'d server-side; rounding is now the caller's responsibility when the REST endpoint is cut over
  - `latestTankReadings` replaces `getLatestTankReadingsPerDevice` — same `distinct: ['deviceId']` + matching `orderBy` approach as `latestWateringEvents`; the old raw-SQL join for `room_name` is dropped in favor of `TankReading.device` → `Device.room` relation traversal (client asks for `tankReadings { device { room { name } } } }`)
  - `TankReading.device` relation resolver null-guards on `root.deviceId` since (unlike `WateringEvent.deviceId`) `TankReading.deviceId` is nullable in `schema.prisma`
  - Smoke-tested end to end against `redwood-db` via `yarn rw dev api` + curl: list, device-filtered list, and create all confirmed working, including nested `device { room { name } }` and `Decimal` → `Float` serialization for `gallons`
- **Tank sensor metrics** — `tankSensorMetrics.sdl.ts`, `api/src/services/tankSensorMetrics/get.ts`
  - `tankSensorHealthMetrics(deviceId, days)` replaces `getTankSensorHealthMetrics` — no Prisma model backs this (pure aggregate over `tank_readings.raw_value`), so it's raw SQL via `db.$queryRaw(Prisma.sql\`...\`)` with `Prisma.sql`/`Prisma.empty` for the optional device filter, same CTE structure as the original Kysely query. Returns `null` when no rows match (mirrors the old "no sensor data found" case)
  - `tankReadingDailyStdDev(deviceId, days)` replaces `getDailyReadingStandardDeviation` — same raw-SQL approach
  - **Gotcha caught by smoke test, not type-check:** Postgres aggregates returning `numeric` (`AVG`, `STDDEV_POP`) come back through `$queryRaw` as `Prisma.Decimal` and need `.toNumber()`, but `PERCENTILE_CONT` returns `double precision` and comes back as a plain `number` — calling `.toNumber()` on it throws at runtime (`row.median_value.toNumber is not a function`). TypeScript didn't catch this because the raw-row type was hand-written and wrong, not inferred. Verified actual return types with `SELECT pg_typeof(...)` before fixing. Lesson: for `$queryRaw`, don't guess column JS types from the SQL function name alone — check `pg_typeof` on the real query.
  - Smoke-tested end to end: default/no-filter, device-filtered + wide window (cross-checked totalCount/min/max/mean against a direct SQL query — exact match), and the empty-result → `null` case
- **Environment readings** — `environmentReadings.sdl.ts`, `api/src/services/environmentReadings/{get,create}.ts`
  - `latestEnvironmentReading(metric, roomName)` replaces `getLatestEnvironmentReading` — unlike the tank sensor metrics domain, this one *doesn't* need raw SQL: Prisma's JSON filtering (`readings: { path: ['metric'], equals: metric }`) replaces the old `readings->>'metric' = $1` raw condition, and the room-name filter becomes a relation filter (`room: { name: roomName }`) instead of a `LEFT JOIN ... AND r.name = $2`. Same semantics as the original (row must have a matching room when `roomName` is given, unfiltered otherwise)
  - `createEnvironmentReading` replaces `appendEnvironmentReading`
  - `EnvironmentReading.room` relation resolver (real FK, lazy per-row, same shape as `Device.room`); `EnvironmentReading.device` relation resolver matches by `deviceId` string (no formal FK, same as `WateringEvent.device`/`TankReading.device`) — `deviceId` is `NOT NULL` here so no null-guard needed, unlike `TankReading.device`
  - Smoke-tested end to end: unfiltered latest, room-filtered latest (confirmed against real `office`/`living_room` data with real metrics `temperature_f`/`humidity_pct`/`pressure_hpa`/`iaq`/`gas_kohm`), the no-match → `null` case, and create
- **Device health stats** — `deviceHealthStats.sdl.ts`, `api/src/services/deviceHealthStats/get.ts`
  - `deviceHealthStats` replaces `getDeviceHealthStats` — multi-table aggregate (devices/rooms/environment_readings/tank_readings/watering_events/device_logs) with no backing Prisma model, so it's raw SQL via `db.$queryRaw` (no dynamic params, so no `Prisma.sql` fragment composition needed here, unlike tank sensor metrics)
  - Still joins `device_logs` for the error count even though the logs *domain* itself is deferred — the table and data already exist from Diff 1, this just reads it, no CRUD/SDL exposure of logs required
  - `friendlyName`/`deviceType` modeled as non-null (`String!`) since both are `NOT NULL` in the current `schema.prisma` (see the Devices backfill note above); `roomDisplayName` stays nullable since it comes through a `LEFT JOIN`
  - Verified all `::int`-cast columns come back as plain `number` via `pg_typeof` before trusting them (per the tank-sensor-metrics gotcha)
  - Smoke-tested end to end against real data for all 4 seeded devices; cross-checked `envReadings7d`/`tankReadings7d` against direct `COUNT(*)` queries — exact match
- **AI summaries** — `aiSummaries.sdl.ts`, `api/src/services/aiSummaries/{get,create}.ts`
  - `latestAiSummary` replaces `getLatestAiSummary`; `createAiSummary` replaces `saveAiSummary`
  - Simplest domain so far — no relations, no raw SQL, straight Prisma `findFirst`/`create`
  - Smoke-tested end to end: fetched the real latest summary, created a new one, confirmed it became the new latest
- **Photo reactions** — `photoReactions.sdl.ts`, `api/src/services/photoReactions/{get,upsert,delete}.ts`
  - `photoReactions(photoFilename)` replaces `getPhotoReactions` — old raw SQL did `GROUP BY reaction` + `array_agg` of `COALESCE(display_name, email-local-part)` in one query; Prisma's `groupBy` doesn't support `array_agg`, so instead of raw SQL this fetches flat rows + a batched `user.findMany({ email: { in: [...] } })` and groups in JS. Returns `PhotoReactionSummary[]` (`reaction`, `users`, `count`), matching the old shape exactly — cross-checked byte-for-byte against the equivalent raw SQL query (order aside)
  - `upsertPhotoReaction` replaces `upsertPhotoReaction` — uses the compound unique Prisma auto-generates from `@@unique([photoFilename, userEmail])` (`photoFilename_userEmail`) as the upsert key; `createdAt` still gets bumped on conflict-update, matching the old `doUpdateSet({ reaction, created_at: new Date() })`
  - `removePhotoReaction` replaces `removePhotoReaction` — uses `deleteMany` (not `delete`) so it stays idempotent/no-throw when the row doesn't exist, matching the old fire-and-forget `tryMutate` semantics; returns `Boolean!` since there's no natural entity to hand back
  - Smoke-tested end to end against real seeded data (`photo1.jpg`, 2 reaction groups): grouped query, upsert-as-insert, upsert-as-update-on-conflict (confirmed same row `id`, not a duplicate), remove, and idempotent double-remove
- **User display names** — `users.sdl.ts`, `api/src/services/users/get.ts`
  - `users` replaces `getAllDisplayNames`; `user(email)` replaces `getUserDisplayName`
  - Dropped the old dual-key map shape (`{ [email]: name, [localPart]: name }`) — that was a caller-side convenience in the old JS, not intrinsic to the data. Client now gets the plain `User[]` (or a single `User` by email) and builds whatever lookup it needs, same as `photoReactions` already does with its own `db.user.findMany` call
  - Read-only domain, no mutations (old `dao.ts` has none for `users` either — presumably managed out-of-band)
  - Smoke-tested against the real 3 seeded users: full list, single lookup, and the not-found → `null` case
- **Device presence** — `devicePresence.sdl.ts`, `api/src/services/devicePresence/upsert.ts`
  - `upsertDevicePresence(deviceId, ipAddress)` replaces `upsertDevicePresence` — mutation only, no query. The old `dao.ts` has no standalone getter either; the only read of this table happens inside the not-yet-migrated `getDevicesWithStatus` big-join query
  - `ipAddress` modeled `String!` (not nullable, matching the old JS signature's write-time contract) even though the underlying column is nullable — same precedent as `CreateWateringEventInput`/`CreateTankReadingInput`
  - `device_presence` was empty in the seeded data (0 rows, matches the old db), so this smoke test exercised both the insert path and the update-on-conflict path (re-upserted the same `deviceId` with a new IP, confirmed single row + refreshed `lastBoot`, not a duplicate); table restored to empty afterward
- **Device config** — `deviceConfig.sdl.ts`, `api/src/services/deviceConfig/{get,update}.ts`
  - `deviceConfig(deviceId)` replaces `getDeviceConfig`; `updateDeviceConfig(deviceId, patch)` replaces `updateDeviceConfig` — both look up by the `deviceId` business key (string), not the internal `id`, since that's what the old REST layer used
  - `updateDeviceConfig` stays raw SQL (`db.$executeRaw`) — this is a genuine case where Prisma's query builder can't express the semantics, not just an old habit: the old code does an atomic `config = jsonb_strip_nulls(config || patch)`, a merge-patch where setting a key to `null` in the patch *deletes* that key rather than storing null. Prisma can only replace a JSON field wholesale, so a query-builder version would need read-merge-write (extra round trip, race-prone) instead of one atomic statement
  - Both resolvers default to `{}` when the device has no config or isn't found, matching the old function's never-throws/always-an-object behavior; return type is `JSON!` (never null)
  - Smoke-tested against real device configs: read a device with an existing key (`tank_capacity_gallons`), read an empty one, patch-merge onto an empty config, patch-merge a new key onto a device that already had one (confirmed the old key survived), and null-patch to delete a key — all matched expected `jsonb_strip_nulls`/`||` semantics exactly; original config values restored afterward
- **Chat sessions/messages** — `chatSessions.sdl.ts`, `api/src/services/chatSessions/{get,create,update}.ts`, `api/src/services/chatMessages/create.ts`
  - `chatSession(sessionKey)` replaces `getChatSession`; `createChatSession` replaces `createChatSession`; `setChatSessionSummary` replaces `setChatSessionSummary`; `appendChatMessage` replaces `appendChatMessage`
  - No standalone `chatMessages(sessionKey)` query — covered by `chatSession(sessionKey) { messages { ... } }` via the `ChatSession.messages` relation resolver, same "no dedicated resolver needed" reasoning as `getRoomIdForDevice` back in the Rooms domain
  - `createChatSession` replicates the old `ON CONFLICT (session_key) DO NOTHING` via `db.chatSession.upsert({ where, create, update: {} })` — an empty `update` is a legal no-op, confirmed by smoke test that calling it twice with a different `personName` keeps the original
  - `recentChatSessions(limit)` replaces `getRecentSessionSummaries` — but returns raw `ChatSession[]` (filtered to non-null `summary`, ordered `startedAt desc`) instead of the old pre-formatted display strings (`"[Jul 8 (with Nico)]: ..."`). Same reasoning as dropping `getAllDisplayNames`'s dual-key map: date/name formatting is caller-side presentation, not data shape — whoever ports the LLM-prompt-building code in `chatContext.ts` later can format it however that prompt needs
  - **Known behavior difference:** `setChatSessionSummary` (`.update()`) and `appendChatMessage` (`connect: { sessionKey }`) will throw if the session doesn't exist, whereas the old Kysely versions silently no-op (`updateTable().where()` affecting 0 rows; `INSERT ... SELECT` from an empty result set). Same trade-off already accepted for `updateWateringEvent`/`updateDevice` elsewhere in this migration — consistent, not a one-off
  - Smoke-tested end to end: create (+ duplicate-create no-op check), get, append two messages, relation-traversal fetch, set summary, and `recentChatSessions` ordering — confirmed `ON DELETE CASCADE` also cleaned up the test messages when the test session was deleted afterward
- **Home knowledge (pgvector)** — `homeKnowledge.sdl.ts`, `api/src/services/homeKnowledge/{get,create,update,delete}.ts`
  - `homeKnowledge` replaces `getAllHomeKnowledge`; `createHomeKnowledge`/`updateHomeKnowledge`/`deleteHomeKnowledge` replace their `dao.ts` namesakes; `searchHomeKnowledge(embedding)` replaces `searchHomeKnowledge`
  - This is the domain the pgvector Known Issue above was written for. In practice it means: `db.homeKnowledge.findMany`/`.delete()` work fine via the query builder (the `Unsupported("vector(768)")` column is simply invisible to Prisma Client — not selected, not settable, doesn't error), but create/update/search all touch `embedding` and so go through `$queryRaw`/`$executeRaw` with the vector formatted as `[v1,v2,...]` text cast to `::vector`, exactly like the old Kysely code did
  - The GraphQL `HomeKnowledge` type has no `embedding` field at all (no caller ever read it back — even the old `HomeKnowledge` TS interface in `types.ts` omits it despite the Kysely query doing `selectAll()`), so this isn't a gap, it mirrors existing behavior
  - **Preserved an existing foot-gun on purpose:** the old `updateHomeKnowledge` unconditionally overwrites `embedding` — an update call that omits it *clears* any previously stored embedding rather than leaving it alone. Kept identical rather than "fixed," and called out with a code comment plus a smoke test that specifically exercises it, so it doesn't get silently fixed/changed later without that being a deliberate decision
  - Smoke-tested end to end against the real 42-row seeded table: similarity search using a real stored embedding (row `id=2` correctly came back as its own closest match, confirming `<=>` cosine distance + the HNSW index work through Prisma's raw SQL), create with a synthetic 768-dim vector (verified `vector_dims(embedding) = 768` directly in Postgres), the list query (confirmed no `embedding` field leaks into the response), update-without-embedding clearing the stored vector (the foot-gun above, confirmed via direct SQL), and delete — table back to exactly 42 rows afterward
- **Chat evals** — `chatEvals.sdl.ts`, `api/src/services/chatEvals/{create,update}.ts`
  - `createChatEval` replaces `insertChatEval`; `updateChatEvalRating` replaces `updateChatEvalRating`
  - The old REST handler manually validated `field` was `'quality'`/`'correctness'` before calling into `dao.ts`; that's now a GraphQL `ChatEvalRatingField` enum (`QUALITY`/`CORRECTNESS`) instead of a raw string, so invalid values get rejected by the GraphQL layer itself before the resolver even runs — no hand-written validation needed
  - Smoke-tested end to end: create, rate quality (confirmed `correctness` stayed untouched), rate correctness independently (confirmed both values persisted together), and confirmed an invalid enum value is rejected with a GraphQL validation error rather than reaching the resolver
- **Photos** *(not a `dao.ts` port — filesystem-backed, no DB table)* — `photos.sdl.ts`, `api/src/services/photos/{get,create}.ts`, `api/src/functions/media.ts`
  - Replaces three old Express routes: `GET /api/list-images` → `photos(limit)` query; `POST /api/admin/upload` (multer) → `createPhoto(input)` mutation; `GET /api/media/:filename` → a Redwood custom Function at `api/src/functions/media.ts`, not a GraphQL resolver, since it returns raw image bytes
  - **Upload mechanism was a real fork, not an obvious call:** Redwood Functions are Lambda-style handlers (`event`/`context`, no Express `req` stream), so the old `multer` middleware doesn't just drop in — a Function-based upload would need manual multipart parsing (e.g. `busboy`). Went with a GraphQL mutation taking base64 (`createPhoto({ filename, dataBase64 })`) instead, on your call — avoids multipart parsing entirely, returns the created `Photo` like every other mutation in this migration, and needed no config changes: tested with a real 1.3MB (1.7MB base64-encoded) photo through GraphQL Yoga's default request handling and it went through with no size-limit error
  - Both the list and create paths use `process.env.UPLOAD_PATH` exactly like the old code (currently `/app/secure-private-storage/family`, a container path — for local `yarn rw dev api` outside docker, override it, e.g. `UPLOAD_PATH=$(pwd)/secure-private-storage/family yarn rw dev api`)
  - `Photo.url` is `/.redwood/functions/media?filename=...` — this only resolves through the web dev server's proxy (the `apiUrl` setting in `redwood.toml`), not by hitting the api server's port directly. The api server itself mounts the function at plain `/media`; confirmed both paths work as expected (proxied path is what a browser will use, direct path is what I used to smoke-test)
  - `createPhoto` sanitizes the incoming filename with `path.basename()` before use, and rejects non-image extensions — the old multer upload trusted `file.originalname` as-is with no such check. A deliberate hardening, not a fidelity requirement, since there's no old behavior to preserve here (multer's default filename handling isn't something to faithfully replicate)
  - Like every resolver in this migration, `media.ts` doesn't enforce auth yet (the Redwood auth stub in `api/src/lib/auth.ts` always returns authenticated) — but call this out specifically once real auth is wired up, since it's a plain Function, not a GraphQL resolver, so `@requireAuth` won't apply to it automatically the way it will for `photos`/`createPhoto`
  - Smoke-tested end to end against the real `secure-private-storage/family` directory: list, upload a real photo (byte-for-byte `md5sum` match against the source file), fetch it back through `media.ts` (content-type, content-length, and `md5sum` all matched), and confirmed path traversal (`../../../etc/passwd` → 400, extension stripped so it never even attempts the read), missing file (404), disallowed extension (400), and missing query param (400) are all handled correctly

**Deferred:**
- App/api/device/system logs (`getSystemLogs`, `appendApiLog`/`appendAppLog`/`appendDeviceLog`, `pruneOldLogs`) — skipping the mechanical port for now since we're likely switching logging to Redwood's own logger/framework rather than re-implementing the old `api_logs`/`app_logs`/`device_logs` tables + union-query pattern as-is. Revisit once we've decided what the new logging story looks like.
- Service stats (`getServiceStats`) — same reason, it's built entirely on `api_logs`/`app_logs` queries, so it's blocked on the same logging-strategy decision.

**Not yet migrated (dao.ts):** none. Every `dao.ts` domain is now either ported (see Done above) or explicitly deferred (see Deferred above — logs/service stats, blocked on the logging-strategy decision) or skipped as dead code (`vision_people` — table doesn't exist in the old db, see the tank-readings entry above).

**Not yet migrated (non-dao.ts server/api functionality) — verified against `server/api/index.ts` routes, not guessed:** vision enrollment (`POST /api/vision/enroll`, multipart), vision SSE stream (`GET /api/vision/stream`), TTS proxy (`POST /api/tts` → Kokoro), host stats (`GET /api/stats`, via `systeminformation`), weather proxy (`GET /api/weather`, via Open-Meteo). `calendar.ts`/`homeActions.ts` (`server/api/`) are not REST routes at all — they're plain function libraries the LLM chat layer calls as tools (get calendar events, control lights, water plants), not something with its own endpoint to port; they'll come up when `chatContext.ts`'s tool-calling gets migrated. Photos (this entry) was pulled out of the "not yet migrated" list because it was asked for explicitly — the rest above is still unstarted.

### Diff 2 (in progress)

Porting the rest of `server/` (everything outside `server/database/dao.ts`, which Diff 1 covered) file by file into the Redwood `api/` tree.

**Architecture decision: where does a persistent background service (not a GraphQL request) live in Redwood?**
- Redwood supports a custom `api/src/server.ts` (via `yarn rw setup server-file`, which scaffolds it and adds `@redwoodjs/api-server` as an explicit `api/package.json` dependency) — this is the officially-supported hook for running arbitrary startup code alongside the API process.
- **Caveat that changed the plan:** `api/src/server.ts` is only used by `yarn rw serve` (production). `yarn rw dev` uses its own dev-mode bootstrap (`nodemon` + `rw-api-server-watch`) and does **not** invoke `server.ts` at all — confirmed by grepping the CLI source (`server.ts` only referenced from `serve.js`/`serveApiHandler.js`, not `dev.js`).
- So the actual initialization hook used is a **side-effect import of `src/lib/mqtt` from `api/src/functions/graphql.ts`** — that file is loaded in both dev and serve/production (including under a custom `server.ts`, since `createServer()` still auto-discovers and mounts all functions), so it's the one place guaranteed to run in both modes. `server.ts` itself stays as the stock scaffolded template; it wasn't the actual trigger point here, but it's still needed for `yarn rw serve` to be a valid deploy target going forward.

**Done:**
- **`server/pubsub/mqttService.ts` → `api/src/lib/mqtt.ts`** (imported for its startup side effect from `api/src/functions/graphql.ts`)
  - Same MQTT client/topic-subscription/message-handling shape as the original, but the old `dao.ts` calls are replaced with the Redwood equivalents built in Diff 1: `appendWaterLevel` → `createTankReading` (services/tankReadings/create.ts, reused directly), `updateWaterEvent` → `updateWateringEvent` (services/wateringEvents/update.ts), `appendEnvironmentReading` → `createEnvironmentReading` (services/environmentReadings/create.ts), `upsertDevicePresence` → `upsertDevicePresence` (services/devicePresence/upsert.ts), `getDeviceConfig` → `deviceConfig` (services/deviceConfig/get.ts)
  - `getRoomIdForDevice` and `appendDeviceLog` have no dedicated resolver (the former was dropped in Diff 1 in favor of the `device.room` relation; the latter's domain is deferred) — both reimplemented as small direct-Prisma helpers in `mqtt.ts` itself rather than round-tripping through GraphQL-shaped resolvers for a single-field lookup / simple insert
  - **`device_logs` vs. deferred "logs" domain — these are different things.** The Diff 1 deferral was about *our own server's operational logging* (`app_logs`/`api_logs`, replaceable by Redwood's logger). `device_logs` is physical IoT device telemetry (ESP32s reporting their own log lines over MQTT) — real domain data that `deviceHealthStats.deviceLogErrors7d` already depends on. So `appendDeviceLog` was **not** deferred here; it's a direct `db.deviceLog.create(...)` write, kept in scope
  - All of `mqttService.ts`'s own operational logging (`appLog(...)` calls — "Connected to MQTT Broker", "Failed to persist tank reading", etc.) replaced with Redwood's structured `logger.info`/`warn`/`error` instead of writing to `app_logs`. This is the first concrete step on the logging-strategy switch mentioned when we deferred the logs domain — not a full migration of that decision, just this one file's own logs no longer go through the old table
  - **Reusing GraphQL resolver functions directly as library code has a real type-system cost, discovered via type-check (not by inspection):** resolver return types (`ResolverTypeWrapper<T>`) allow each field/array-element to individually be `T | Promise<T>`, since GraphQL resolvers are allowed to resolve fields lazily. That's invisible when you only `await` the call and use the result as a whole (fine for `createTankReading`, `updateWateringEvent`, `createEnvironmentReading`, `upsertDevicePresence`, `deviceConfig` here — none of their results get destructured), but breaks direct property access — reusing `tankReadings` (the list query) for `canWaterPlants`'s "get latest reading" lookup failed to type-check on `readings[0].pctFull`. Fixed by querying Prisma directly (`db.tankReading.findFirst(...)`) for that one call site instead of fighting the resolver's type; documented in a code comment so it doesn't look like an arbitrary inconsistency later.
  - `shared/plant_config.json` (topic name constants) needed `resolveJsonModule: true` + `"../shared"` added to `api/tsconfig.json`'s `include` — that directory sits outside the Redwood-generated tsconfig's project boundary by default. Extended rather than duplicating the topic constants into a second TS file, since `shared/` is explicitly meant to be the cross-service source of truth (hardware config generation reads it too).
  - `mqtt` added as an explicit `api/package.json` dependency (`yarn workspace api add mqtt`) — wasn't in the workspace at all before this.
  - Smoke-tested end to end against the **real** `mqtt-broker` container (not a mock): connected + subscribed to all 10 topics; published real messages for pump-cycle-complete (verified `watering_events` row updated), water-level (verified calibration math: 35% of a 20-gallon tank → 7.00 gallons, matching a real device's stored `config`), device-boot (verified `device_presence` upsert), device-logs (verified `device_logs` insert), and a temperature reading (verified `environment_readings` insert with the room correctly resolved via the direct-Prisma `getRoomIdForDevice` helper — confirmed against a real device→room mapping). Used a temporary `yarn rw g script` (deleted after) to exercise `publishWaterCommand`'s safety-block path (low tank level → `BLOCKED_LOW_WATER`, confirmed) and its allowed path (healthy tank level → command actually published and received on the real `plants/{device_id}/pump` topic).
  - **Caused a real side effect during testing:** the "allowed path" test published a genuine `WATER_ON` command to the live pump topic, which ran the real Office Water Tower pump for 10 seconds. Sensor-report topics are safe to simulate; `plants/{device_id}/pump` is a live actuator command, not a passive topic — should have asked before publishing to it. Noted for next time: cap any such test at 10s, and check first.
  - All test rows (`watering_events`, `tank_readings`, `device_presence`, `device_logs`, `environment_readings`) cleaned up afterward; row counts verified back to baseline.

**Not yet migrated:** everything else under `server/` — `server/api/*.ts` (the Express REST routes, including the not-yet-ported items listed above), `server/database/schema.ts`/`migrate.ts`/`migrations/*` (superseded by `schema.prisma` + Prisma migrations from Diff 1, but not yet deleted), `robot-vision-publisher/`, `robot-vision-worker/` (separate services, not part of the `server/` Express app).

This closes out Diff 1 of Phase 2 modulo the deferred logging domains. Next steps to consider: wiring these resolvers up to actual frontend pages/consumers, deciding the new logging strategy so `getSystemLogs`/`getServiceStats` can be ported, and porting `chatContext.ts`'s business logic (LLM prompt building, MQTT handlers) that calls into these services rather than the raw `dao.ts` functions.

### Diff 2 continued — audit checkpoint (2026-07-10)

We got cut off mid-session before this doc was updated with everything that actually landed. Auditing the working tree against this doc before resuming, so this checkpoint is accurate as of now, not aspirational.

**Additional "Done" beyond what's written above (all uncommitted, in the working tree):**
- **Ollama** — `server/api/ollama.ts` → `api/src/lib/ollama.ts` (`ollamaChat`, `ollamaChatWithTools`, `getEmbedding`)
- **Chat business logic** — `server/api/chatContext.ts` → `api/src/lib/chatContext.ts` (`runChatTurn`, `buildSystemPrompt`, `summarizeSessionAsync`) — this is the piece Diff 1's closing note flagged as not-yet-ported; it's done now. Confirmed it's wired as a consumer of the Diff-1 services (`homeKnowledge`, `chatSessions`, `chatMessages`), not raw `dao.ts` calls, and also pulls in `homeActions.ts`/`calendar.ts` as LLM tool libraries (see below).
- **Chat GraphQL surface** — `chat.sdl.ts` (`chatSystemPrompt` query, `sendChatMessage`/`startChatSession` mutations) + `api/src/services/chat/{get,mutations}.ts` — replaces `/api/chat`, `/api/chat/session`, `/api/chat/session/:key` (the last via `chatSessions.sdl.ts`'s `ChatSession.messages` relation), `/api/chat/context`
- **Home knowledge onboarding** — `server/api/homeKnowledgeOnboarding.ts` → `api/src/lib/homeKnowledgeOnboarding.ts` + `homeKnowledgeOnboarding.sdl.ts` + services — replaces `/api/home-knowledge/coverage`, `/questions`, `/answers`
- **Home actions (LLM tools)** — `server/api/homeActions.ts` → `api/src/lib/homeActions.ts` (`waterPlants`, `getLightEntities`, `controlLight`) — not a REST route in the old app either, just a function library `chatContext.ts` calls; ported the same way
- **Calendar (LLM tool)** — `server/api/calendar.ts` → `api/src/lib/calendar.ts` (`getCalendarEvents`, `getDOECalendar`, `addCalendarEvent`) — same "function library, not a route" shape as homeActions
- **AI summary hourly job** — `api/src/lib/aiSummaryService.ts` — replaces the old `startHourlySummary()` call at the bottom of `server/api/index.ts`; wired as a side-effect import from `api/src/functions/graphql.ts` (same pattern as `src/lib/mqtt`), so it starts under both `yarn rw dev` and `yarn rw serve`

**Audited against the live Express routes in `server/api/index.ts` (not just `dao.ts`) — here's what's actually left, superseding the older "Not yet migrated" lists above:**

1. **Auth** — `api/src/lib/auth.ts` is still the stock Redwood stub (`isAuthenticated` always `true`). The old `server/api/auth.ts` (CF-Access-header check + `ADMIN_EMAILS` gating on `/admin` routes) has no Redwood equivalent yet — every `@requireAuth` resolver in the new API is currently wide open. **Doing this next.**
2. **Logging** — still deferred, per the original decision above (`getSystemLogs`, `getServiceStats`, `api_logs`/`app_logs` tables) — blocked on picking a new logging strategy, not revisited in this audit.
3. **Express-only endpoints with no GraphQL equivalent needed** — `/api/health`, `/api/config`, `/api/display-stats` (token-auth for the physical display board, intentionally bypasses CF Access), `/api/whoami`, `/api/stats` (host stats via `systeminformation`), `/api/weather` (Open-Meteo proxy), `/api/tts` (Kokoro proxy), `/api/vision/stream` (SSE — a GraphQL subscription could replace it eventually, but nothing does today).
4. **Real business-logic gaps that are *not* just auth/logging/express-only** (found during this audit, not previously called out):
   - **`getDevicesWithStatus`** (`/api/devices/status`) — the dashboard's big per-device aggregate (health/last-seen across `device_presence`+`device_logs`+`environment_readings`+`tank_readings`, latest env readings, calibrated tank gallons, OTA flag). No SDL/service exists for this yet — it's a genuinely unported `dao.ts` query, not merely deferred.
   - **`/api/can-water`** — combines water-level + device-config + cooldown-history checks to compute whether the UI's water button should be enabled. Not ported.
   - **`/api/admin/water`** — the on-demand "admin clicks water now" mutation (6-hour cooldown check + `createWateringEvent` + `publishWaterCommand`). `createWateringEvent` exists as a mutation, but nothing in GraphQL calls `publishWaterCommand` — there is currently no way to actually trigger a pump through the new API. (`waterPlants()` in `homeActions.ts` does the automated/all-pumps version as an LLM tool, but that's a different code path from this on-demand single-device one.)
   - **Validation dropped in the port:** `createDevice`/`updateDeviceConfig` mutations lost the old routes' extra checks (`device_id` alphanumeric-with-hyphens regex; numeric range checks on `tank_capacity_gallons`/`calibration_raw`/`calibration_gallons`). GraphQL's `String!`/`Int!` covers presence but not these constraints. Not blocking, but worth a decision on where this validation should live going forward.
   - **`vision/people` + `vision/enroll`** depend on a `vision_people` table that doesn't exist in the DB — already flagged as dead code earlier in this doc, re-confirming here so it doesn't get accidentally implemented as if it were real functionality during this pass.
5. **Photos/media, photo reactions, users, AI summaries, home knowledge CRUD, list-images** — all confirmed already ported (see Diff 1 "Done" above) — re-verified against the live routes during this audit, no gaps found in these domains.

### Diff 3 — Auth (2026-07-10)

Replaced the `api/src/lib/auth.ts` stub with a real port of the old `server/api/auth.ts` Express middleware.

**How it works:** Cloudflare Access sits in front of nginx/api and injects `cf-access-authenticated-user-email` once a user has authenticated via SSO — there's no login flow inside this app and nothing to decode (no JWT verification here, same trust boundary as the old middleware, which also just read the header raw). `ADMIN_EMAILS` (`nries1@gmail.com`, `avalongoebel@gmail.com`) gates admin-only actions, same list as before.

**Why the standard Redwood auth hooks didn't fit:** `createGraphQLHandler`'s `getCurrentUser`/`authDecoder` options only run when a request carries an `auth-provider` header + a matching `Authorization`/cookie — the plumbing Redwood's bearer-token providers (dbAuth, Netlify, Supabase, etc.) rely on. Cloudflare Access sends neither; it just injects the email header unconditionally. Traced this through `@redwoodjs/api`'s `getAuthenticationContext` (returns `undefined`, and `getCurrentUser` never fires, without one of those headers) before concluding the standard hook was the wrong extension point.

**What actually wires it up:**
- `cfAccessAuthPlugin` (`api/src/lib/auth.ts`) — a custom Envelop plugin hooking `onContextBuilding` unconditionally (not gated behind the auth-provider-header check), registered via `createGraphQLHandler({ extraPlugins: [cfAccessAuthPlugin] })` in `api/src/functions/graphql.ts`. Reads the CF header off the raw event with `@redwoodjs/api`'s `getEventHeader` (handles both the Lambda-style and Fetch-`Request` event shapes) and sets `context.currentUser = { email, roles }`.
- `isAuthenticated`/`hasRole`/`requireAuth` in `auth.ts` read `context.currentUser` via Redwood's ambient `context` (`@redwoodjs/context`) — the existing `requireAuth` GraphQL directive (`api/src/directives/requireAuth`) needed no changes, since it already just calls `applicationRequireAuth({ roles })` from this file.
- **Deliberately left the plugin's parameter types unannotated** — `extraPlugins: Plugin[]`'s `Plugin` type comes from graphql-server's own *nested* `graphql-yoga` copy, not the top-level one, so importing `Plugin` from the top-level package (needed to add it as an explicit dependency) produced bogus version-skew structural errors; typing against `RedwoodGraphQLContext` directly also failed because the generic `Plugin<YogaInitialContext>` at that hook position doesn't statically know about Redwood's `event` field even though it's always there at runtime (confirmed Redwood's own built-in auth plugin does the exact same unchecked `context.event` access). Left untyped rather than lying about the shape with a cast — `noImplicitAny` isn't on in this project's `tsconfig.json`, so this is a real, deliberate choice, not an accident.
- **`media.ts` needed its own guard** — it's a plain Function, not a GraphQL resolver, so `@requireAuth` never applied to it automatically (the file already had a comment flagging this from when it was written). Added a `getCurrentUserFromEvent(event)` check at the top, 401s if missing — same "any authenticated user, not admin-only" behavior as the old `/api/media/:filename` route (it came after `app.use(authMiddleware)` but wasn't under `/admin`).
- **Applied `roles: ["admin"]` to the mutations whose old routes lived under `/api/admin/*`**: `createDevice`/`updateDevice`/`deleteDevice` (`devices.sdl.ts`), `updateDeviceConfig` (`deviceConfig.sdl.ts`), `createPhoto` (`photos.sdl.ts`, replaces the old `/api/admin/upload`), `createWateringEvent` (`wateringEvents.sdl.ts`, replaces `/api/admin/water`'s append side — the mutation still doesn't call `publishWaterCommand`, see gap #3 above, so this is DB-write-only for now). `updateDevice`/`deleteDevice` have no old-route precedent (net-new since Diff 1's CRUD completion) — admin-gated by analogy with `createDevice`, since they're the same class of device-admin operation. `updateWateringEvent` stays plain `@requireAuth`: its only caller is `mqtt.ts`'s internal completion callback, which imports the resolver function directly and bypasses the GraphQL/directive layer entirely, so the role wouldn't protect anything there even if set — no old client-facing route ever exposed it either. Everything else that was **not** under `/admin` in the old app (photo reactions, chat, home-knowledge CRUD) was deliberately left at plain `@requireAuth` — any authenticated (non-admin) user could already do those, and admin-gating them now would be a real behavior regression, not a fidelity fix.
- Added `requireAuth.test.ts` cases for the new real behavior (throws with no `currentUser`, throws when required role is missing, passes when present) — the scaffolded stub test asserted the *old* always-true behavior and would have been a false pass otherwise.

**Smoke-tested end to end** against the real `redwood-db` via `yarn rw dev api` + curl (jest's `yarn rw test api` couldn't run in this environment — its `globalSetup` fails on `DATABASE_URL` parsing even though the value looks correct; a pre-existing environment gap, not something this change caused, not chased further given the working `yarn rw dev` + curl smoke-test pattern already used throughout this migration): no header on a `@requireAuth` query → `UNAUTHENTICATED`; non-admin header on the same query → succeeds; non-admin header on `createDevice` → `FORBIDDEN`; `nries1@gmail.com` on `createDevice` → succeeds (row created and cleaned up afterward); header matching is case-insensitive (`NRIES1@GMAIL.COM` works); `media.ts` with no header → 401, with a header → falls through to the normal 404-for-missing-file path.

**Still not done:** the Express-only endpoints and the `getDevicesWithStatus`/`can-water`/`admin-water` gaps from the audit above are untouched by this diff — this was auth infrastructure only.

### Diff 4 — Watering commands (2026-07-10)

Ports gaps #2 and #3 from the Diff 2 audit checkpoint above: `/api/can-water` and the on-demand `/api/admin/water` trigger.

**Where these live, and why:** neither is CRUD, so neither got forced into `create`/`get`/`update` naming. Followed the two precedents already established elsewhere in this migration:
- **`canWater`** (`api/src/graphql/canWater.sdl.ts` + `api/src/services/canWater/get.ts`) — its own service directory, same shape as `deviceHealthStats`/`tankSensorMetrics`: a computed status spanning multiple tables (tank readings, device config, watering history, current-user admin status), not owned by any single domain.
- **`waterDevice`** (`api/src/services/wateringEvents/waterDevice.ts`, new mutation added to the existing `wateringEvents.sdl.ts`) — lives in the `wateringEvents` service dir because it *does* create a `WateringEvent`; it just also has a side effect (`publishWaterCommand`) that the plain `createWateringEvent` CRUD mutation doesn't. Reuses `createWateringEvent` directly as a library call, same precedent as `homeActions.ts`'s `waterPlants()`.

**`canWater`** — left at plain `@requireAuth`, not admin-gated, on purpose: the old route let any authenticated user ask "can I water?" and got `{ canWater: false, disabledReasons: ["Admin access required"] }` back rather than a hard 403 — the UI shows a disabled button with a tooltip reason, not an error page. Reproduces all three old checks (admin status, tank level vs. 3-gallon floor using the device's `tank_capacity_gallons` config for the message, cooldown vs. `SHARED.pump_cycle_cooldown_hours`), each independently try/caught with `logger.warn` on failure — same per-check resilience as the old route (a transient DB hiccup on one check shouldn't blank out the whole response).

**`waterDevice`** — admin-gated (`@requireAuth(roles: ["admin"])`), matching the old `/api/admin/water` route. Notable differences from a pure port, both deliberate hardening given this triggers a real pump and the old route had none of these:
- **Duration is clamped to 5–120 seconds server-side.** The old route trusted client-supplied `duration_seconds` with zero bounds checking.
- **`action` is hardcoded to `SHARED.water_action_on` server-side**, not client-supplied — there's only ever been one action value in practice (`WATER_ON`; no `WATER_OFF` exists anywhere in the codebase), so accepting an arbitrary action string from the client was unnecessary surface for zero benefit.
- **`startedBy` comes from `context.currentUser.email`**, not a client-supplied field — matches the old route's actual behavior (`req.userEmail`, server-derived, never trusted from the request body) more faithfully than `createWateringEvent`'s own `CreateWateringEventInput.startedBy: String!` does today (that field *is* client-suppliable, a pre-existing minor gap from Diff 1, not touched here).
- The cooldown check uses `SHARED.pump_cycle_cooldown_hours` (6h) instead of the old route's hardcoded `1000 * 60 * 60 * 6` literal — numerically identical today, just no longer a second magic-number copy of the same constant.
- Cooldown only blocks on the most recent event having `status === 'complete'` — matches old behavior exactly: `'requested'` events (nothing has reported completion yet) never block a retry, only device-confirmed-complete ones do.

**Smoke-tested end to end** via `yarn rw dev api` + curl against the real `redwood-db`: `canWater` unauthenticated → `UNAUTHENTICATED`; non-admin → `{ canWater: false, disabledReasons: ["Admin access required"] }`; admin → `{ canWater: true, disabledReasons: [] }`. `waterDevice` non-admin → `FORBIDDEN`; admin → created a real `WateringEvent` row, `startedBy` correctly `nries1@gmail.com`, and **`publishWaterCommand`'s own low-water safety block (`canWaterPlants`, a DB-level check independent of broker connectivity) fired and marked both test rows `BLOCKED_LOW_WATER`** — so no MQTT publish was even attempted. Separately confirmed the `mqtt-broker` hostname is unreachable from this dev sandbox (12 reconnect attempts logged, `EAI_AGAIN`), meaning even without that safety block, nothing could have reached the real pump here — two independent reasons this test carried no physical-actuation risk. Test rows (`watering_events` ids 86, 87) deleted afterward via direct `psql`.

**Still not done:** `getDevicesWithStatus` (gap #1) is the only item left from the Diff 2 audit checkpoint.

### Diff 5 — `getDevicesWithStatus` (2026-07-10)

Closes gap #1, the last item from the Diff 2 audit checkpoint. `devicesWithStatus.sdl.ts` + `api/src/services/devicesWithStatus/get.ts`, replacing the dashboard's big per-device aggregate behind `GET /api/devices/status`.

No backing Prisma model — it's a correlated-subquery join across five tables (`devices`, `rooms`, `device_presence`, `device_logs`, `environment_readings`, `tank_readings`, `watering_events`) plus tank-calibration math in `CASE` expressions, `json_object_agg`, and `row_to_json`. Stayed raw SQL via `db.$queryRaw`, same precedent as `deviceHealthStats`/`tankSensorMetrics` — this isn't a case where Prisma's query builder was being avoided out of habit, it's a genuine escape-hatch case (aggregation shapes and calibration math the query builder can't express).

**Verified raw-row JS types empirically rather than assuming from the SQL** (per the standing rule from the tank-sensor-metrics gotcha) — two things a naive reading of the query would get wrong:
- `jsonb` columns (`config`) and `json_object_agg`/`row_to_json` results (`latest_env_readings`, `latest_tank_reading`, `latest_pump_event`) come back auto-parsed into plain JS objects by the driver — no manual `JSON.parse` needed, and no `Prisma.Decimal` wrapping either, since the values already passed through a JSON serialize/deserialize round-trip server-side (unlike the tank-sensor-metrics case, where raw `numeric`/`double precision` columns came back Decimal-wrapped or not depending on the SQL function).
- **Timestamps behave differently depending on whether they pass through `row_to_json`.** Top-level timestamp columns (`last_boot`, `last_seen`, `latest_env_timestamp`) come back as real JS `Date` objects, same as any other Prisma raw-SQL timestamp column. But the `timestamp` fields *nested inside* `latest_tank_reading`/`latest_pump_event` — because they're serialized through `row_to_json` — come back as **ISO strings**, not `Date` objects (JSON has no native date type). The row type reflects this split explicitly (`Date | null` vs `string`) rather than typing everything the same. Redwood's `DateTime` GraphQL scalar accepts both transparently, so no extra conversion was needed — just got the raw-row type right so a future callsite doesn't assume `.getTime()` works on the nested ones and crash at runtime.
- `config`/`latest_env_readings` are typed `Prisma.JsonValue` (from `@prisma/client`), not `Record<string, unknown>` — the generated `JSON!` scalar type expects `JsonValue` specifically, and TypeScript's structural check on the recursive `JsonValue` union doesn't accept a plain `Record<string, unknown>` in its place.

Query name is `devicesWithStatus` (no `get` prefix), matching the plural-noun convention already used everywhere else (`devices`, `wateringEvents`, `latestTankReadings`, etc.) rather than the old function's `getDevicesWithStatus` name.

**Smoke-tested end to end** against the real `redwood-db` via `yarn rw dev api` + curl: unauthenticated → `UNAUTHENTICATED`; authenticated → all 4 real devices returned with plausible values (env readings, tank calibration math, pump event history, `healthy` correctly `false` for all four since none have reported in the last 12 hours in this seeded dataset). Cross-checked the calibrated-tank-gallons math for `E6B740` (no calibration data, so the `ELSE` branch: `pct_full/100 * tank_capacity_gallons` = `16/100 * 20` = `3.2`) against a standalone `psql` query of the same expression — exact match.

This closes out all four gaps identified in the Diff 2 audit checkpoint (auth, `canWater`, `waterDevice`, `devicesWithStatus`). Remaining known-deferred items are still just the Express-only endpoints (health/config/display-stats/whoami/stats/weather/tts/vision-stream) and the logging domain (`getSystemLogs`/`getServiceStats`), both called out earlier in this doc.

### Diff 10 — Photo Gallery page + a real photo-reaction auth gap found while building it (2026-07-12)

`web/src/pages/PhotoGalleryPage/` ported from `html/photo-gallery.html`: admin-gated upload section (`createPhoto`, base64 over GraphQL rather than multipart — see Diff 1's note on why), the photo grid, and per-photo reaction overlays (`PhotoCard.tsx`) with the old page's long-press-for-picker / quick-click-for-default-reaction behavior, including one faithfully-preserved quirk: a long press still fires the overlay's own click handler right after (mousedown+mouseup on the same element always dispatches a click), so the default reaction gets saved *and* the picker opens to immediately override it — not fixed, since it's harmless and is what the old page actually did, not a guess.

**Real, pre-existing auth gap found and fixed, not just ported around:** `upsertPhotoReaction`/`removePhotoReaction` (Diff 1) took `userEmail` as a plain client-supplied GraphQL argument, completely untrusted — any authenticated user could upsert or delete a reaction *as any other user* by passing an arbitrary email. Same class of gap as `createWateringEvent.startedBy` (noted in Diff 1 as "a pre-existing minor gap, not touched"), except this one was on the direct path being newly wired up to a real UI for the first time, same reasoning that justified fixing the `/vision/stream` tracking gap in Diff 9. Fixed by dropping `userEmail` from `UpsertPhotoReactionInput` and from `removePhotoReaction`'s args entirely, deriving it server-side from `context.currentUser.email` instead (`api/src/services/photoReactions/{upsert,delete}.ts`) — matching the precedent already set by `waterDevice.startedBy` in Diff 4.

**Another instance of the Diff 9 stale-path bug, caught by grep before it could bite:** `services/photos/{get,create}.ts` hardcoded `Photo.url` as `/.redwood/functions/media?filename=...` — the same assumed-rewrite-proxy prefix that Diff 9 found broken for `apiUrl`. Fixed to a bare `/media?filename=...`, matching how `server.ts` actually mounts that Function. Grepped the whole repo for the `.redwood/functions` string afterward to confirm no other instance was hiding.

**Smoke-tested against the real deployed container** (rebuilt image, `docker exec`-verified the running container's bundle/dist actually contained the new code before testing — the lesson from Diff 9's stale-cache incident) via Playwright against the real `secure-private-storage/family` directory and real seeded `photo_reactions` rows: gallery renders all real photos with correct reaction badges and tooltips (screenshotted); admin upload section visible and a real upload round-tripped end-to-end (image appeared in the grid); quick-click reaction correctly upserted under the authenticated user's own row (not a duplicate); long-press picker opened correctly.
- **Test cleanup gap worth knowing about:** the quick-click test's "before" value was captured and precisely restored (`🥰`) afterward. The long-press test's click-quirk (see above) also silently overwrote a second real reaction row, whose original value was *not* captured beforehand — attempted to recover it from the old pre-redwood `db` service (kept per its own docker-compose comment as a rollback safety net), but that volume turned out to not exist in this environment (`docker compose up -d db` created a brand-new empty volume rather than attaching to real historical data) — removed that empty container/volume again rather than leave a fake "safety net" in place. **Net effect: one real reaction row (`1779325625325-IMG_5961.jpeg`, `nries1@gmail.com`) is currently set to 🥹 and its prior value is unknown** — low-stakes (a personal reaction emoji, on the account's own row), but flagging explicitly rather than silently leaving it changed. The uploaded test image was deleted from disk afterward (no `deletePhoto` mutation exists to do this via the API).

### Diff 11 — Switch to RedwoodJS Cells (2026-07-12)

All three pages built so far (Home, Devices, Photo Gallery) used plain `useQuery`/`useMutation` + a colocated `queries.ts`, following the pattern `DevicesPage` happened to establish in Diff 8 — not Redwood's actual idiomatic data-fetching convention, which is **Cells** (`FooCell.tsx` exporting `QUERY`/`Loading`/`Empty`/`Failure`/`Success`, auto-wired into a `useQuery` + loading/empty/error state machine by a Redwood babel/vite transform). Converted all existing query call sites to Cells, per your request to standardize on the idiom going forward.

**New cells** (all under `web/src/components/`, matching where `yarn rw g cell` scaffolds by default — verified once via the actual generator to confirm the exact import/export shape before hand-writing the rest):
- `DevicesWithStatusCell` — replaces `DevicesPage`'s main `useQuery` (still polls every 30s via `beforeQuery` returning `pollInterval`). Renders the device grid and (since it alone has the fetched list) the `DeviceConfigModal`, driven by `configDeviceId`/callbacks passed down as cell props — Cells forward all non-query props straight through to `Loading`/`Empty`/`Success` alongside the query data.
- `RoomsCell` — replaces the `AddDeviceModal`'s room list fetch; its `Success` renders `AddDeviceModal` directly (a cell doesn't have to render a bare data region, it can wrap an entire existing component). `Loading`/`Failure` still render the modal shell so clicking "+ Add Device" gives immediate feedback rather than a silent pause.
- `LatestAiSummaryCell` — the one atypical case. HomePage needs the summary text mixed into its own speech-bubble state machine (config placeholder fallback, greeting overrides, truncation), not rendered as a standalone UI region, so this cell is intentionally "headless": every state (`Loading`/`Empty`/`Failure`/`Success`) renders `null` and reports the value up via an `onSummary` callback prop instead. Flagged in-code as a deliberate deviation from the normal cell shape, not an oversight.
- `PhotosCell` — replaces `PhotoGalleryPage`'s photo-list `useQuery`, parameterized by an optional `limit` prop via `beforeQuery`.
- `PhotoReactionsCell` — replaces `PhotoCard`'s per-photo reactions `useQuery`, parameterized by `photoFilename`. No `Empty` export on purpose: zero reactions is just `Success` with an empty array, not a distinct state worth its own UI (the default `isEmpty` check only diverts to `Empty` when one is actually exported).
- `PhotoCard` moved from `web/src/pages/PhotoGalleryPage/` to `web/src/components/PhotoCard/`, since it's now consumed by `PhotosCell` (a top-level component) rather than only its own page.

**Mutations dropped their manual refetch plumbing in favor of Apollo's `refetchQueries`**, which is what actually made moving `DeviceConfigModal`/`AddDeviceModal` behind separate cells (`DevicesWithStatusCell` vs. `RoomsCell`) tractable — the two modals no longer need an `onCreated`/`onSaved` callback threaded back up to a `refetch()` living in a sibling cell's closure; `createDevice`/`updateDeviceConfig`/`createPhoto` each just declare `refetchQueries: ['DevicesWithStatusQuery' | 'PhotosQuery']` and Apollo refetches whatever's currently mounted. `upsertPhotoReaction` targets its own single photo's query precisely (`refetchQueries: [{ query: QUERY, variables: { photoFilename } }]`, importing the sibling cell's own `QUERY` export) rather than the by-name form, since by-name would refetch every photo's reaction cell on the page for a change to just one.

**Type reconciliation, not just a passthrough:** the codegen'd `DevicesWithStatusQuery` type comes back with `config`/`latestEnvReadings` typed as the GraphQL `JSON` scalar (`Prisma.JsonValue`), but `DeviceCard`/`DeviceConfigModal` (unchanged from Diff 8) expect the narrower hand-written `DeviceConfig`/`EnvReadings` shapes for ergonomic field access. Cast once at the cell boundary (`DevicesWithStatusCell`'s `Success`) rather than either loosening those components' types or casting at every field read inside them.

**One real mistake made and caught during this diff, not swept under the rug:** while sanity-checking that `yarn rw type-check`'s piped output actually surfaces errors (as opposed to silently succeeding, which a couple of earlier commands in this session appeared to do), appended a deliberate bogus line to `DevicesPage.tsx` and then ran `git checkout -- web/src/pages/DevicesPage/DevicesPage.tsx` to undo it — since the file had uncommitted changes from this diff's refactor (never committed, per this doc's standing rule), `git checkout` reverted the *entire* file back to its last-committed, pre-Cells state, not just the one bogus line. Caught immediately (the file's new content was visibly the old pre-refactor version) and rewritten correctly. Lesson: use a targeted edit to undo a targeted edit, not `git checkout --` on a file with other uncommitted work in it.

**Verified the directory-style cell import (`import FooCell from 'src/components/FooCell'`, resolving to `FooCell/FooCell.tsx` with no `index`) actually works** rather than assuming from memory — first via `yarn rw type-check web` (confirmed the check reliably surfaces real errors first, via the bogus-line experiment above, so a clean run afterward could be trusted), then via `yarn rw build`/`docker compose build` succeeding, then via `docker exec`-grepping the deployed bundle for known strings before browser-testing (per the Diff 9 lesson about not trusting a clean build alone).

**Smoke-tested end to end against the real deployed container** (Playwright, `cf-access-authenticated-user-email` header spoofed, same as every prior diff): all three pages screenshot cleanly with zero console/network errors. Home page's headless cell correctly feeds the real AI summary into the speech bubble. Devices page renders all 4 real devices through `DevicesWithStatusCell`; opening "+ Add Device" through `RoomsCell` populated the real room list (3 options) and closed cleanly without saving. Photo gallery renders all real photos through `PhotosCell`/`PhotoCard`/`PhotoReactionsCell`; a quick-click reaction through the new mutation-plus-`refetchQueries` path correctly updated the tooltip in place — captured the exact "before" value first this time and restored it afterward (learned from this same diff's earlier photo-reaction test, see above).

### Diff 12 — Plant Care page (2026-07-12)

Ported from `plant-care.html`. Continues the Cells convention from Diff 11 — six new cells (`RoomSelectCell`, `EnvironmentTempCell`, `TankStatusCell`, `SensorHealthCell`, `CanWaterCell`, `WateringHistoryCell`) plus a shared `Card` component matching `html/css/dashboard.css`'s look (this is the first page ported from that stylesheet rather than the Tailwind-CDN pages — the `dash-*` color tokens Diff 8 added to `web/src/index.css` for exactly this were still unused until now).

**Chart library decision (flagged as open back in Diff 8):** asked directly rather than guessing — landed on **Recharts**, added as a new `web/` dependency, on the reasoning that this app will need more charts later (System Health's planned CPU/mem gauges) so the investment pays off beyond this one page.

**Real gap found and worked around, not silently copied:** the old REST endpoint (`server/database/dao.ts`'s `getRoomsWithDevices`) filtered each room's device list to `device_type = 'pump'` only. The new generic `Room.devices` relation (Diff 1) has no such filter — it returns every device in the room. Every real room here has both a pump and an environment sensor, so naively taking `devices[0]` (what the old page did, trusting the REST layer's pre-filtering) would sometimes select the sensor instead of the pump for tank/water-related queries. Fixed by filtering to `deviceType === 'pump'` client-side in `RoomSelectCell` rather than touching the shared `rooms` schema (which the Devices page also depends on, unfiltered, correctly).

**A second old-page bug found and fixed, not reproduced:** `renderLastWateringCallout` compared `log.status === 'COMPLETE'` (uppercase). Real status values are lowercase (`complete`/`requested`/`BLOCKED_LOW_WATER`, confirmed against real data) — so that check never matched anything, and the old page's "Last Watering" callout silently fell back to the most recent event regardless of status. `WateringHistoryCell` uses the correct lowercase value.

**One deliberate layout change:** the old page's "X updates this week" line lived in the Water Tank Status card, but its data always came from the same fetch as the Sensor Health card's stats (`loadSensorHealth()` populated a DOM element that visually lived in a different card). Moved it into `SensorHealthCell` itself, next to the data it's actually drawn from, instead of re-wiring a cross-cell callback just to keep an old, slightly arbitrary layout split.

**`deviceConfig(deviceId: String!)` is non-null, unlike every other query on this page** (`tankReadings`/`canWater`/`wateringEvents`/`tankSensorHealthMetrics` all accept a nullable `deviceId` — "no filter" is a valid state for them). Rather than fight that mismatch with an `@include(if:)` directive (which doesn't relax the underlying variable's non-null type requirement at validation time), `TankStatusCell` just requires a real `deviceId` prop, and the page only mounts it (alongside `CanWaterCell`) when the selected room actually has a pump device — showing a plain "No pump device in this room" message otherwise. Every real room currently has one, so this is a defensive edge case, not something exercised by real data today.

**Dropped, not ported, and why:**
- The old page's "Moisture Sensors" card was hardcoded fake demo data (`Device 1 (Fiddle Leaf): 65%`, literally never wired to a fetch) — there's no soil-moisture-per-plant sensor type in this schema at all (`hardware/` only has `plant-node`'s tank/pump sensor and the separate `environment-sensor`). Not carried forward, same reasoning as skipping `vision_people`/`saveInterval` elsewhere in this doc.
- The "Scheduling" card's "Cycle Interval (Hours)" input and its "Update" button called `saveInterval()` — a function never defined anywhere in the old page's script. Genuinely dead, non-functional markup, not a feature to reproduce.

**Manual Override duration bounds updated to match reality:** the old input allowed 10-90s with no server-side check; `waterDevice` (Diff 4) actually clamps 5-120s server-side. `CanWaterCell`'s input now advertises 5-120 so the UI doesn't claim bounds the server doesn't enforce. Also replaced the old page's blocking `alert(result.message)` after watering with an inline message below the button, consistent with how every other admin action in this app (Devices' modals) already surfaces mutation results.

**Smoke-tested end to end against the real deployed container**, same Playwright/spoofed-header approach as every prior diff: both real rooms (Living Room, Office) render correctly with real temperature/tank/history data, and switching rooms via `RoomSelectCell` correctly re-fetches every other cell with the new device/room. The Sensor Health chart and stats legitimately render empty for both rooms — confirmed via direct SQL, not assumed: the most recent `tank_readings` row is 15 days old for the Living Room pump and 8 days old for the Office pump, both outside the `tankSensorHealthMetrics`/`tankReadingDailyStdDev` 7-day window, so an empty chart is the real, correct answer for this seeded data, not a query bug. **Did not click "WATER NOW"** — that mutation triggers a real pump via MQTT, and per standing instruction this app's command topics don't get exercised without asking first, broker-reachability aside.

### Interlude — `mqtt-broker` wasn't running on the real host (2026-07-12)

Between Diff 12 and Diff 13, testing surfaced that `redwood`'s MQTT client had been failing to connect (`ENOTFOUND mqtt-broker`) throughout every diff in this doc so far — previously assumed to be a sandbox limitation (per the standing note repeated since Diff 4), but you confirmed this session is the actual production home server, so it warranted a real investigation rather than another assumption.

**Root cause: no `mqtt-broker` container existed on this host at all** — not stopped, not present under any name, confirmed via `docker ps -a` and `ss -tlnp` showing nothing bound to 1883/9001. **Found a real structural collision while digging in:** this repo's `docker-compose.yml` and the separate pre-migration `~/papu` checkout's `docker-compose.yml` both hardcode identical `container_name`s (`mqtt-broker`, `homeassistant`, `ollama`, `kokoro`, `robot-vision-worker`) mapped to the same host ports — Docker won't run two containers with the same name, so **only one of the two stacks can have these services up at a time**. Whichever one you last brought up wins the physical devices' connections; this isn't something either compose file guards against today.

**Started `mqtt-broker` here** (`docker compose up -d mqtt-broker`, ports were free, no conflict) and confirmed the fix is real, not just theoretical: `redwood`'s client connected (`/health` → `mqtt: ok`), and a real watering cycle you ran on the Office pump round-tripped correctly — `waterDevice`'s queued command (`requested`, from before the broker existed) got delivered on reconnect, the pump ran it, and the completion callback wrote a real `complete` row into `redwood-db` (confirmed via direct SQL: ids 88/89, `E6B740`). Passive environment telemetry (temperature/humidity/pressure/IAQ) also resumed for both env sensors within the same window. Tank-level (`tank_readings`) specifically hadn't produced a new row by the time this was checked — plausibly just a longer/different reporting cadence on that sensor, not re-investigated further since you were satisfied with the confirmation already in hand.

**Not resolved, flagged for later:** the `container_name` collision between `~/papu` and this repo isn't fixed — it wasn't asked for, and fixing it (e.g., dropping the fixed names, or deciding this repo owns these services going forward) is a real decision about the cutover timeline, not something to make unilaterally mid-diff.

### Diff 13 — System Health page (2026-07-12)

Ported from `system-health.html`, scoped exactly as Diff 8 flagged it back when this page was still a stub: **the log table and per-service request/message-rate stats are dropped, not ported** — both hit `/api/system-logs` and `/api/service-stats`, which Diff 7's logging-strategy decision intentionally retired (no DB-backed operational logging going forward; that's a separate future Grafana/Loki initiative, not something to half-resurrect here). What's left after that scoping is exactly the three pieces Diff 8 called out: service-health dots, the device health table, and the CPU/mem/temp gauges.

**New:** `useHealth`/`useHostStats` hooks (`GET /health`, `GET /stats` — plain Functions, not GraphQL, same `apiFetch` pattern as `useConfig`/`useAdminStatus`), polling at the old page's own intervals (15s, 3s). `DeviceHealthStatsCell` wraps `deviceHealthStats`, polling every 60s (matching the old page's `setInterval(loadDeviceHealth, 60000)`). Styled with the page's own original Tailwind-CDN classes (`bg-slate-800/50 border-slate-700`) rather than the `dash-*`/`Card` treatment — unlike Plant Care, this page was never one of the `dashboard.css` pages, so reusing `Card` would have been importing the wrong page family's look.

**Ported as-is, including one inherited simplification:** the Watering column's "N ok / N blocked / N err" breakdown is `total - complete - errors`, exactly like the old page — "blocked" is really just "neither complete nor logged as an error," not necessarily every bucketed event actually being `BLOCKED_LOW_WATER`. Confirmed this during the real end-to-end test above: Office Water Tower showed "1 ok · 1 blocked" for a window containing one `complete` and one still-`requested` event (the one that had been queued while the broker was down) — the `requested` one isn't actually blocked, it's just neither of the other two buckets. Same subtraction-based math as the original page, not a new bug.

**Smoke-tested end to end against the real deployed container**, and this time against genuinely live data rather than a stale snapshot (thanks to the `mqtt-broker` fix above): API/MQTT tiles both show green/connected, AI correctly shows red/no-activity (no vision worker or publisher running), and the device health table shows real, current counts (228/224 seven-day env readings for the two sensors, the real "1 ok · 1 blocked" watering breakdown for the Office pump). Verified the three different poll intervals actually fire on their own schedules (not just once on mount) by counting real network requests over a 9-second window: 3× `/stats`, 1× `/health`, 1× `/graphql` — consistent with 3s/15s/60s.

### Diff 14 — Home Knowledge page (2026-07-12)

Ported from `home-knowledge.html`. Three cells/components: `HomeKnowledgeCoverageCell` (progress bar + per-category breakdown), `HomeKnowledgeInterview` (page-scoped, not a Cell — it's mutation-driven with no backing query), `HomeKnowledgeFactsCell` (collapsible facts browser with inline add/edit/delete, grouped by subject).

**Dropped, not ported, and why:** the old page's "Recognized people" chips (`GET /api/vision/people`) and "Enroll a new person" upload form (`POST /api/vision/enroll`) both queried a `vision_people` table that doesn't exist in the real database — flagged as dead code back in the Diff 1 `dao.ts` port, re-confirmed here by grepping the whole `api/src` tree for any trace (`vision/people`, `vision/enroll`, `vision_people`) and finding none. Real enrolled-face data lives in `robot-vision-worker/known_faces/encodings.pkl`, written only by the `learn_face.py` CLI — no API surface exists to read or write it from the web app today. Not a regression, just following the precedent already set.

**Inherited, not a new gap:** `createHomeKnowledge`/`updateHomeKnowledge` accept an optional client-supplied `embedding`, but neither this page's add/edit form nor the interview's `processHomeKnowledgeAnswers` path ever sends one — confirmed this matches the *old* REST routes and `dao.ts` exactly (checked `~/papu/server/api/index.ts` and `dao.ts`'s `insertHomeKnowledge`/`updateHomeKnowledge` signatures directly), not something this port changed. Manually-added or onboarding-derived facts have never had embeddings; only the original seeded rows do.

**A real bug found live, not caught by type-check:** `handleSave` built the `processHomeKnowledgeAnswers` mutation's `pairs` input by spreading the question object (`{ ...q, answer }`). Apollo tags every query/mutation result with `__typename` for cache normalization, so that spread carried `__typename` into the input — which `HomeKnowledgeAnswerPairInput` correctly rejects (GraphQL input types don't have `__typename`). TypeScript didn't catch this because `q`'s inferred type includes `__typename` as an optional field already (part of the generated `CellSuccessProps`/mutation-result shape), so the spread type-checked fine; only a real save attempt surfaced the 400. Fixed by constructing the input object explicitly instead of spreading.

**Two issues raised after initial testing, both fixed:**
- **Favicon never actually changed.** `web/src/index.html` — the single shared HTML shell for the whole SPA (unlike the old per-page static files, which each set their own `<link rel="icon">`) — still pointed at the stock Redwood-scaffold `favicon.png`. Copied the real `papu.ico`/`papu.png` (both actually PNG data internally, despite the `.ico` extension — confirmed via `file`) into `web/public/`, pointed the shell's `<link>` tags at them, and removed the now-unused placeholder.
- **Interview auto-fetched questions on every page load.** Removed the mount-time `useEffect` call to `generateHomeKnowledgeQuestions`; added a "Start interview" button so it only fires on demand.

**A real infrastructure gap found while testing the fix above, not a code bug:** with the auto-fetch removed, clicking "Start interview" surfaced "Failed to load questions. Is the LLM online?" — but you confirmed Ollama *is* online. Traced it to the same class of issue as the earlier `mqtt-broker` collision: this repo's `docker-compose.yml` also hardcodes `container_name: ollama` (behind `profiles: ['gpu']`), colliding with `~/papu`'s identically-named `ollama` service — confirmed `ollama` didn't resolve at all from the `redwood` container (`ENOTFOUND`), meaning whatever's answering "is the LLM online?" elsewhere isn't reachable from this stack. Also worth knowing for future work: `generateHomeKnowledgeQuestions`/`processHomeKnowledgeAnswers` never throw on an LLM failure — both wrap the Ollama call in a bare `catch { return [] }` — so this failure mode is a normal 200 with empty data, not a GraphQL error; the frontend has to track "have we attempted at least once" itself to tell "never tried" apart from "tried and got nothing," since `error` alone doesn't fire.
- Started `mqtt-broker`'s sibling here (`docker compose --profile gpu up -d ollama`) — started cleanly with no name conflict, meaning `~/papu`'s wasn't running at the time. Confirmed reachable from `redwood` immediately after (`200` on `/api/tags`).
- It came up with **zero models pulled** (fresh `./ollama` volume, `ollama list` empty). Found a real mismatch before pulling anything: a home-knowledge fact itself claims "Papu uses a locally-hosted Ollama server running the qwen3:4b model," but `.env`'s `OLLAMA_MODEL` is actually `qwen2.5:7b` — asked which was correct rather than guessing (pulling the wrong multi-GB model is expensive to redo). You confirmed `qwen2.5:7b` is correct — the knowledge-base fact is stale. Pulled `qwen2.5:7b` (4.7GB); `qwen3:4b` (2.5GB, pulled first while investigating) is also present now but unused — left in place, not removed, since it's not causing harm and removing it wasn't asked for.

**Smoke-tested end to end against the real deployed container with a genuinely live LLM this time** (GPU-confirmed via `nvidia-smi` — RTX 2070, model resident at 100% GPU): `generateHomeKnowledgeQuestions` returned 10 real, personalized questions referencing actual household facts by name (Arlo's pediatrician, Nico's yoga practice, Avalon's job) — first call took ~21s including model warm-up, no timeout issue in the UI. Facts browser CRUD verified with a real add → edit → delete cycle, including the native `confirm()` dialog firing correctly, and confirmed via direct SQL that no residue was left after delete.
- **One more real thing worth flagging, found by finishing the loop on the interview save test:** `processHomeKnowledgeAnswers` doesn't just extract facts from an answer — given a test answer that was intentionally unrelated noise ("ZZTEST-INTEGRATION: this is a test answer..."), the LLM **fabricated three plausible-looking but entirely fictional facts** (fake emergency-contact phone numbers attributed to Nico, Avalon, and Arlo by name) rather than recognizing the input as noise. This isn't a migration regression — same prompt, same `processAnswers` logic as the old app, untouched by this port — but it's a real characteristic of the feature worth knowing: garbage or off-topic answers can produce confident-sounding hallucinated facts about real household members, not just get silently skipped. Deleted the three fabricated rows immediately after confirming the bug fix worked (verified via `updated_at > now() - interval '10 minutes'`, not guessed).

### Diff 15 — Robot page (2026-07-13)

Ported from `robot.html` — the last of the seven original static pages. Reuses `RobotHead` and `useVisionStream` (both already built for the Home page), plus the headless-cell pattern (`LatestAiSummaryCell`, and two new ones: `DisplayNamesCell`, `ChatHistoryCell`) established across this migration.

**Dropped on purpose, not by oversight — already decided back in Diff 6:** the old page's browser-side camera capture, MediaPipe face detection, and MQTT frame-publishing (`<video>`, `@mediapipe/face_detection`, `client.publish('robot/vision/frame', ...)`). That was flagged as "temporary MVP camera source" scaffolding standing in for a robot camera that doesn't exist yet — only the *viewer* role survives, sourced from `/vision/stream` via `useVisionStream` instead of a direct browser→MQTT connection. This also means the raw `mqtt.js` websocket connection itself is gone; `useVisionStream` gained a `connected` boolean (tracking the `EventSource`'s open/error state) to drive the old page's "● connected/disconnected" indicator, which used to reflect that direct MQTT link.

**A real old-page UI quirk preserved on purpose:** voice exchanges and text-chat exchanges are two separate surfaces even though both hit the same session/backend — voice replies show in a transient "last exchange" transcript panel (You said / Robot) and are spoken aloud, while text-chat sends persist to the scrolling `#chat-log`. Voice exchanges are never added to the persistent log in the UI (though they *are* saved server-side, same as before — reload the page and `chatSession(sessionKey)` would include them). Replicated faithfully rather than merging the two into one unified log, since that's a real behavior difference in the old page, not an accidental omission.

**Pan-sign inconsistency between the two pages that share `RobotHead`, found while porting, not invented:** `index.html`'s abstract nav-hub head uses `rotateY(pan * 60deg)`, but `robot.html`'s uses `rotateY(-pan * 60deg)` — the negation only appears here, presumably because this page originally mirrored a live camera preview and the abstract Home-page head never needed to. Resolved by negating at the call site (`<RobotHead pan={-pose.pan} ...>`) rather than adding a `mirror` prop to the shared component — keeps `RobotHead` itself simple, since only one of its two call sites needs the flip.

**A real race condition found and fixed, not a hypothetical:** the greeting/history-seeding logic initially used two independent one-shot callbacks (`onHistory`, `onSummary`), each checking "has the other one already run" before deciding whether to show a greeting. `LatestAiSummaryCell`'s query fires immediately on mount, while `ChatHistoryCell` can't even mount until `useChatSession`'s async session-creation resolves — so the summary callback reliably fires *first*, sees history hasn't been seeded yet, and bails, with nothing left to re-trigger it once history actually does arrive. Confirmed live (chat log stayed empty indefinitely despite both underlying queries succeeding) before fixing it — replaced the racing one-shot callbacks with two `'pending' | T` state values and a single effect that only acts once both have resolved.

**Infrastructure gap found while testing chat, distinct from the earlier qwen model mismatch:** `sendChatMessage` failed with a generic GraphQL "Something went wrong." — traced to `runChatTurn`'s call to `getEmbedding()` (for home-knowledge RAG context), which requests Ollama model `nomic-embed-text` (hardcoded in `api/src/lib/ollama.ts`) — never pulled, so every chat message failed the same way regardless of what was asked. Pulled it (274MB, small compared to the ~5GB chat models) after confirming with you it was needed; chat works end to end afterward.

**Smoke-tested end to end against the real deployed container**, GPU-backed LLM included: layout renders correctly (head, stats grid, mic button, connection indicator — genuinely green/"connected" now that `mqtt-broker` is up from the earlier session), the boot-time greeting shows the real live AI summary, a real text-chat round trip returns a real `qwen2.5:7b` reply with a real `evalId`, the eval strip renders and a rating click persists via `updateChatEvalRating` (confirmed via the actual GraphQL response, real id). **Not fully exercisable here, and flagged rather than assumed passing:** the voice path (Web Speech API STT + Kokoro/browser TTS) needs a real microphone and user gesture that headless Playwright can't provide — same class of limitation as this migration's standing notes on vision-tracking/physical-device testing. Confirmed the mic button is clickable and produces no console errors, but the actual STT→LLM→TTS loop wants a real browser session to verify.

**This closes out the Frontend Port section opened at Diff 8.** All 7 of the original `html/*.html` pages (Home, Devices, Photo Gallery, Plant Care, System Health, Home Knowledge, Robot) now have real `web/` ports behind the same `redwood` container, each verified against real seeded/live data rather than mocked. `html/`, `server/`, `robot-vision-publisher/`, and `robot-vision-worker/` are all still present and untouched — this doc's `web/`/`api/` work has been built alongside the old stack the whole time, not cut over yet. What's left is genuinely new work, not migration debt: deciding when to retire `html/`/`server/`/nginx for real, the Grafana/Loki observability initiative parked back at Diff 7, and the robot-head hardware work (servo control, on-device publisher) called out in `CLAUDE.md`.

### Diff 6 — Express-only endpoints (2026-07-10)

Ports everything left in the "Express-only, no GraphQL equivalent needed" bucket. "No GraphQL equivalent needed" never meant "leave as Express" — `server/` is going away entirely, so each of these needed a new home in the `api/` workspace regardless. All except one became plain Redwood Functions (`api/src/functions/*.ts`), same shape as `media.ts`. Route paths dropped the old `/api` prefix, matching the `media.ts` precedent (`/api/media/:filename` → `/media`).

**Auth in Functions, one more time:** every one of these (except `health`/`config`/`display-stats`, which were public/token-gated in the old app too) needed its own `getCurrentUserFromEvent` guard — `@requireAuth` is a GraphQL directive, it doesn't apply to Functions, same reasoning already established for `media.ts`.

**Done, one Function each:**
- **`health.ts`** — `GET /health`. Public. Reports `serviceStatus` from `src/lib/mqtt` (already tracks mqtt/vision-worker/publisher liveness, nothing new needed).
- **`config.ts`** — `GET /config`. Public, static env-derived UI bootstrap config.
- **`whoami.ts`** — `GET /whoami`. CF-auth required. The old route's `'anonymous'` fallback was dead code in practice (it sat behind `authMiddleware`, which already 401s before the handler runs) — made that explicit rather than reproducing unreachable code.
- **`adminStatus.ts`** — `GET /admin-status`. CF-auth required (not admin-only — this is how the UI *finds out* whether the user is an admin, so it can't itself require the role). Reuses `user({email})` from `src/services/users/get` (safe to reuse directly — single-object result, not a list, same precedent as `latestEnvironmentReading` reuse elsewhere).
- **`stats.ts`** — `GET /stats`. CF-auth required. Host CPU/mem/temp via `systeminformation`, added as an explicit `api/package.json` dependency (was resolving from a stale hoisted root `node_modules` copy, not a declared workspace dependency — same fix pattern as `mqtt`/`graphql-yoga` earlier in this migration).
- **`weather.ts`** — `GET /weather`. CF-auth required. Open-Meteo proxy. Factored the axios call + weather-code-to-text map into `api/src/lib/weather.ts` (`getCurrentWeather`/`weatherCodeToText`) since `display-stats` needed the exact same call — the old app had this duplicated across both routes verbatim; not copying it a third time.
- **`displayStats.ts`** — `GET /display-stats`. Token-auth via `DISPLAY_TOKEN` query param, deliberately bypassing `getCurrentUserFromEvent`/CF Access entirely — this feeds the physical e-ink display board, not a browser. Reuses `latestEnvironmentReading` directly (safe, single-object), but reads `tank_readings`/`watering_events` via direct Prisma rather than the `latestTankReadings`/`latestWateringEvents` resolvers — same `T | Promise<T>` list-resolver-element gotcha as `waterDevice.ts`'s cooldown check, caught by `yarn rw type-check`, not by inspection.
- **`tts.ts`** — `POST /tts`. CF-auth required. Kokoro TTS proxy, returns raw `audio/mpeg` bytes via `isBase64Encoded: true`, same binary-response shape as `media.ts`.

**One exception — `vision/stream` is not a Function:**
SSE requires holding the raw HTTP response open and writing to it later; Redwood Functions are Lambda-style handlers that return one response and terminate, so there's no way to express this as a Function at all. Presented this fork to you explicitly rather than picking silently, since all three options (real Fastify SSE / switch to polling / defer) had genuine trade-offs — you picked real SSE via a custom Fastify route.

- Registered directly on the Fastify instance via `createServer`'s `configureApiServer` hook in `api/src/server.ts`, reusing `visionListeners` (`src/lib/mqtt`, already broadcasts `robot/vision/result` messages to whatever's in that `Set` — that part was already ported in Diff 2, just never had a way to add a listener until now).
- **`visionListeners`'s type was quietly wrong before this diff** — `Set<Response>` imported from `express`, a package not actually in `api/package.json` and not used anywhere else in this Redwood app; it only type-checked because of a stale hoisted copy in the root `node_modules`. Changed to `Set<ServerResponse>` from Node's built-in `http` module, which is what it actually holds now (Fastify's `reply.raw`).
- **Only takes effect under `yarn rw serve`** (production/docker-compose) — `yarn rw dev` uses its own dev-mode bootstrap that never invokes `server.ts` at all, per the dev/serve split already noted in Diff 2 (`api/src/functions/graphql.ts`'s comment on why `mqtt`/`aiSummaryService` are triggered from a side-effect import there instead of from `server.ts`). There is currently no way to exercise this specific route with `yarn rw dev`.
- **Two real bugs caught only by testing the actual TCP response, not by inspection or type-check:**
  1. `reply.hijack()` is required before writing to `reply.raw` — without it, Fastify still expects the handler to call `reply.send()` and holds the connection open forever, headers never sent. First diagnosed as this and fixed, but the endpoint *still* hung — turned out unrelated: a stale server process from an earlier fix attempt hadn't actually died (`kill` had returned a shell exit code that looked like success but the process it was targeting had already forked into a *different* child pid via `execa`), so the "fixed" code was never actually running during that retest. Found by `lsof -ti:8912` + `ps aux` showing three live processes for one supposedly-killed server, not by anything code-related.
  2. Even after confirming (via `console.log`) that the handler ran, hijacked, and wrote successfully, a raw `socket.recv()` test still showed **zero bytes reaching the client**. Root cause: the old Express route's `res.flushHeaders()` call had been dropped when translating to raw Node `reply.raw.writeHead()` — Node buffers headers until the first body write or `.end()` by default, and `writeHead()` alone doesn't push them to the socket. Added `reply.raw.flushHeaders()` back (Node's `http.ServerResponse` has the same method Express's `res` does) and headers + the initial write arrived immediately.
- Also added an immediate `: connected\n\n` SSE comment line on connect (not in the old route) — ignored by `EventSource` clients per spec, just confirms the stream is live right away instead of the client waiting for the first real vision-recognition event, which could be a long wait.
- **Smoke-tested via `yarn rw build api` + `yarn rw serve api` + raw TCP (curl and a raw Python socket, to rule out curl-side buffering as a false negative)**: no header → `401`; with header → real `200`/SSE headers/chunked body received within milliseconds, confirmed via header dump. The broadcast-to-listeners path itself (`visionListeners` iteration in `mqtt.ts`) was **not** exercised end-to-end — no MQTT broker is reachable from this sandbox (same limitation noted in earlier diffs) — but it uses the exact same `ServerResponse.write()` call already proven to reach the client on this connection, so this is a reasoned inference from a shared code path, not an independently-verified round trip. Worth a real end-to-end check once this deploys somewhere the vision publisher/worker pipeline is actually running.

**Who actually calls this — checked after the fact, not assumed.** Grepping this repo for `vision/stream`/`EventSource` turns up nothing outside the route definition itself — `html/robot.html`'s live tracking/recognition display connects **directly to the MQTT broker over WebSockets** (`mqtt.js` in-browser, `ws://<host>:9001`), bypassing the API server entirely. So this endpoint currently has zero real consumers.

That's not dead code, though — it's the correct building block for where this is headed, not where it is today. `robot.html` today conflates two roles that only exist together because there's no physical robot camera yet: (1) a temporary MVP camera source (publishes the visitor's own webcam frames to `robot/vision/frame` at 3fps) and (2) a viewer of tracking/recognition results. The camera-source role is throwaway dev scaffolding — recognition accuracy/speed is already thoroughly validated and doesn't need a browser-webcam stand-in anymore — and won't be ported into the React app; a real robot head gets its own on-device publisher (same shape as `robot-vision-publisher/publisher.py`, on the local network, same trust model as the ESP32 plant nodes — no browser, no internet hop, no auth story needed, which is what actually delivers low-latency recognition). The viewer role is what survives into the React port, and it should consume `/vision/stream` rather than connect to MQTT directly: **the Mosquitto broker has no auth at all** (`allow_anonymous true`, no ACL file, port 9001 mapped straight to the host in `docker-compose.yml`) — any client that can reach it has read/write access to *every* topic, including `plants/{device_id}/pump`, the same actuator topic `waterDevice` (Diff 4) carefully gates behind `@requireAuth(roles: ["admin"])`. `/vision/stream` is read-only and rides the same CF-Access auth as the rest of this API, so it doesn't reopen that hole. `html/robot.html` itself is untouched for now — left as-is until the React port actually happens, decision revisit deferred to that point.

**Function-vs-Fastify-route interaction verified, not assumed:** confirmed the custom `/vision/stream` route coexists cleanly with Redwood's own function-discovery routing (`/:routeName` and `/:routeName/*`, which Fastify's router prioritizes as more-specific-wins over static paths regardless of registration order) — re-tested `/config` and a real GraphQL query against the same running `yarn rw serve` process immediately after the SSE route responded correctly, both still worked.

**Environment note:** `systeminformation` needed adding to `api/package.json` (see `stats.ts` above) — same "resolves via hoisting but isn't a declared workspace dependency" issue as `mqtt`/`graphql-yoga` earlier in this migration; always check `api/package.json` explicitly rather than trusting that an import resolving means it's properly declared.

**Still not done:** the logging domain (`getSystemLogs`/`getServiceStats`) is the only remaining deferred item — blocked on picking a new logging strategy, per the original Diff 1 decision.

### Diff 7 — Logging decision, and closing out the migration (2026-07-10)

The logging strategy Diff 1 deferred on is now decided, and it turns out to resolve the deferral rather than extend it.

**The decision:** no DB-backed operational logging going forward. App-level warn/error events already moved to Redwood's built-in `logger.warn`/`logger.error` (pino → structured JSON on stdout, captured by `docker-compose logs`) consistently across every diff in this migration — that part wasn't really a pending decision by this point, just a pattern already in continuous use since `mqtt.ts` in Diff 2. What was still genuinely blocked — the dashboard's log-viewer page (`getSystemLogs`) and service-stats page (`getServiceStats`) — turned out not to need a DB-backed answer either: visualization/debugging is the actual goal, not "restore the `api_logs`/`app_logs` tables specifically." That reframes it as a separate observability initiative (likely Grafana + Loki + Promtail — our stdout JSON logs are already Loki-ready with zero application code changes, and Grafana can chart domain data like tank levels/device health straight from Postgres), explicitly parked for a future session rather than built now.

**Cleanup, since dropping the DB requirement means the old tables are genuinely dead, not just deferred:**
- Removed the `ApiLog`/`AppLog` models from `schema.prisma` — confirmed via grep first that nothing in `api/src/` ever referenced them (no services, no SDL — the Diff 1 deferral meant these were carried into `schema.prisma` from the old DB but never wired up to anything in the new app).
- Dropped the underlying `api_logs`/`app_logs` tables via a real Prisma migration (`20260710042902_drop_unused_api_app_logs`) — **not** just a schema edit. `prisma migrate dev` wouldn't run non-interactively in this environment (same limitation as the pgvector HNSW index workaround earlier in this doc), so the migration file was hand-written in the same format Prisma generates, then applied with `prisma migrate deploy` (the non-interactive-safe command) and verified with `\dt` against the real `redwood-db`.
- **This was a real, confirmed-before-acting deletion of existing data** — `api_logs` had 4,217 rows, `app_logs` had 23,900, both leftover from the Diff 1 seed copy (not live — nothing in the new app writes to them, so this wasn't destroying anything in active use, but it was still real row data, called out before running the migration rather than silently dropped).
- **`device_logs` is untouched, on purpose** — verified via `\dt` after the migration. This is real IoT device telemetry (ESP32s reporting their own log lines over MQTT), not our own server's operational logging — that distinction was already established back in Diff 2 (`appendDeviceLog` was explicitly *not* deferred, unlike `app_logs`/`api_logs`) and still holds; nothing about this decision touches it.

**This closes out the migration.** Every domain from the original Diff 1/2 scope, every gap found in the Diff 2 audit checkpoint, every Express-only endpoint, and now the logging decision are all done. What's left is exclusively new/follow-on work, not migration debt: wiring these resolvers to real frontend pages (the `web/` side is still the stock Redwood scaffold), the robot head initiative (servo control, speech, TTS — see `CLAUDE.md`), the Grafana/Loki observability project parked above, and porting `html/` to React (which is what surfaced the `/vision/stream` vs. direct-MQTT architecture question earlier in this diff). `server/`, `robot-vision-publisher/`, and `robot-vision-worker/` are all still present and still what's actually running in production — this doc's `api/` work has been built alongside them, not cut over yet, consistent with the "don't touch the old thing" approach used throughout.

Note: `dao.ts` also has `getVisionPeople`/`upsertVisionPerson` against a `vision_people` table that doesn't exist in the old db (`papu-db-1`) — pre-existing dead code, not something this migration introduced. Flag before porting "vision people" rather than silently modeling a table that isn't there.

**Patterns established (apply to future domains):**

- Service directories are **plural** (`api/src/services/devices`, not `device`) — canonical Redwood convention.
- SDL field names and nullability should mirror `schema.prisma` exactly. Don't write DTO mappers — for fields that genuinely diverge (relations, renames), use a field resolver: `export const <TypeName>: <TypeName>RelationResolvers = { ... }` in the service file. It's applied automatically to every query/mutation returning that type, not per-operation.
- Redwood's generated resolver map types (`QueryResolvers['x']`, `MutationResolvers['x']`) are optional properties, so indexing them directly yields `Fn | undefined` and breaks destructured-arg type inference in the editor. Always wrap: `NonNullable<QueryResolvers['x']>`.
- Offset pagination convention: `(page, rows, order)` args on list queries — `limit = rows ?? <domain default>`, `offset = page ? page * limit : 0`, `order` is `'ASC' | 'DESC'` (case-insensitive) mapped to Prisma's `orderBy` direction.
- Multiple `.sdl.ts` files can each declare their own `type Query { ... }` / `type Mutation { ... }` — Redwood merges them, so no need to centralize.
- For computed/aggregate results with no backing Prisma model (stats, health checks), use `db.$queryRaw(Prisma.sql\`...\`)` with `Prisma.sql`/`Prisma.empty` for optional filter fragments — same escape hatch as the pgvector workaround, just for aggregates Prisma's query builder can't express (`STDDEV_POP`, `PERCENTILE_CONT`, mode, etc.). Always verify raw-row column JS types with `SELECT pg_typeof(...)` against the actual query rather than assuming from the SQL function name — see the tank sensor metrics gotcha above.
- Don't reach for raw SQL just because the old `dao.ts` used it — a lot of the old raw SQL exists because Kysely has no JSON-path or relation-filter helpers, not because Postgres needs it. Check whether Prisma's query builder already covers it before writing `$queryRaw`: JSON field filtering (`{ path: [...], equals }`) and relation filtering (`{ room: { name } }`) replaced two raw-SQL conditions in `latestEnvironmentReading` with plain Prisma calls.

**Environment notes:**

- `yarn` isn't on `PATH` by default in this environment — run `corepack enable` (picks up the `packageManager: yarn@4.6.0` pin in `package.json`).
- Root `package.json` needed a manual `"gql": "yarn rw g types"` script — Redwood doesn't ship one out of the box.
- VSCode must be set to the workspace TypeScript version (`Cmd/Ctrl+Shift+P` → `TypeScript: Select TypeScript Version` → "Use Workspace Version"), otherwise the editor can show false-positive type errors that `tsc`/`yarn rw type-check` don't reproduce.

## Frontend Port — html/ → web/ (React)

The API migration above closed out in Diff 7. The next real work, per that closing note, is wiring the ported resolvers to an actual frontend — `web/` was still the stock Redwood scaffold, and `html/*.html` (7 static pages) still hits the old Express REST routes, most of which no longer exist. This is planned as one diff per page (following the same pattern as the API migration), starting with shared infrastructure + the two simplest/most foundational pages.

### Diff 8 — Shared infra + Devices page + nav-hub Home page (2026-07-10)

**Shared infrastructure (`web/`):**
- **Tailwind CSS v4** — `yarn add` resolved v4, not v3, which changes the setup: no `tailwind.config.js`/PostCSS, instead `@tailwindcss/vite` as a Vite plugin (`web/vite.config.mts`) and CSS-first `@theme` config in `web/src/index.css`. Renamed `vite.config.ts` → `vite.config.mts` — with `.ts`, Vite's config loader tried to bundle `@tailwindcss/vite` (ESM-only) as CJS and failed at startup (`ESM file cannot be loaded by require`); caught by actually running `yarn rw dev`, not by type-check. `@theme` declares `--color-dash-*` tokens matching `html/css/dashboard.css`'s CSS custom properties, so pages ported from that stylesheet (rather than the Tailwind-CDN pages) can match their palette exactly in later diffs.
- **`DashboardLayout`** (`web/src/layouts/DashboardLayout/DashboardLayout.tsx`) — the shared header/back-link every page in this port uses; none of the 7 html pages had a shared nav component before.
- **`Modal`** (`web/src/components/Modal/Modal.tsx`) — shared overlay wrapper (click-outside-to-close), first used by the two Devices-page modals, will be reused by later CRUD-heavy pages.
- **`apiFetch`** (`web/src/lib/apiFetch.ts`) — thin fetch wrapper for the plain Redwood Functions (`config`, `whoami`, `adminStatus`, `stats`, etc.), which aren't GraphQL and don't go through Apollo. Uses `globalThis.RWJS_API_URL`, the same global Redwood's own Apollo client resolves internally, so the api base stays consistent across both paths.
- **`useAdminStatus`/`useConfig`** (`web/src/hooks/`) — wrap `GET /adminStatus` and `GET /config`. **Function routes are named by camelCase filename, not the old kebab-case REST path** — `useAdminStatus` initially called `/admin-status` (matching the old `/api/admin-status` route) and got a 404; caught by actually running the page in a browser (`node_modules/playwright`'s Chromium via `web-agents/`, no `chromium-cli` in this sandbox) and reading the network log, not by type-check or inspection. Fixed to `/adminStatus`, matching `api/src/functions/adminStatus.ts`'s filename.
- **`relativeTime`** (`web/src/lib/format.ts`) — replaces the copy-pasted `relativeTime()` from `devices.html`/others.

**Devices page** (`web/src/pages/DevicesPage/`) — chosen as the first real page: self-contained (no MQTT, no chart library, no missing backend, unlike `system-health.html` — see below), and it exercises the two patterns every later CRUD page needs: polling (`useQuery(..., { pollInterval: 30_000 })`, replacing the old manual `setInterval`) and admin-gated mutations behind modals.
- Read side: `devicesWithStatus` + `rooms` queries, ported 1:1 from `devices.html`'s card grid (badges, room, IP, last boot/seen, env-reading/tank-level/pump-event sections keyed off `deviceType`).
- Write side, gated behind `useAdminStatus().isAdmin` (hides both the "+ Add Device" button and per-card "Edit" tank-capacity buttons) — **`devices.html` itself had no client-side admin gate at all**, relying only on server-side enforcement; this port adds one, closing that gap rather than reproducing it.
  - **Add Device modal** → `createDevice` mutation.
  - **Device Settings modal** (tank capacity + calibration) → `updateDeviceConfig(deviceId, patch)`, reproducing the null-clears-the-key merge-patch semantics from Diff 1 (clearing calibration sends `{ calibration_raw: null, calibration_gallons: null }`, not an omitted key).
- **Real backend gap found and fixed, not worked around:** `devices.sdl.ts`'s `Device` type and `CreateDeviceInput` never exposed `deviceType`/`hasOta`, even though `schema.prisma`'s `Device` model has both (`device_type`/`has_ota` columns, confirmed against real seeded rows via `psql`) and the old `/api/admin/devices` POST route set both. Extended the SDL (`Device.deviceType: String!`, `Device.hasOta: Boolean!`, `CreateDeviceInput.deviceType: String!`, `CreateDeviceInput.hasOta: Boolean!`) — no service-code change needed, `createDevice`/`updateDevice` already pass `input` straight through to `db.device.create`/`update`. Without this, every device created through the new Add Device form would have silently defaulted to `device_type='unknown'`, breaking the type-specific card rendering that already depends on it.
- **Also discovered, not fixed (out of scope for this diff):** `CreateDeviceInput` requires `model: String!` and `hardwareVersion: String!`, neither of which the old REST form ever collected (checked real rows: `model` is a hardware board id like `esp32`/`metroesp32-s3`, unrelated to `deviceType`). Added both as real form fields (`hardwareVersion` defaults to `'v1'`, matching the Diff 1 backfill precedent) rather than guessing a hardcoded value — small, necessary form addition, not a scope decision that needed deferring.

**Home page** (`web/src/pages/HomePage/`) — deliberately minimal: a nav hub linking to all 6 other pages, nothing else. `index.html`'s animated robot head / speech bubble / MQTT vision-tracking was **not** ported here — it's shared with `robot.html` and depends on the SSE-vs-MQTT architecture decision already written into this doc's Diff 6 (`/vision/stream` should replace raw MQTT subscribe for the viewer role); building a throwaway version now would be rework once that diff does it properly.

**Stub pages:** `PlantCarePage`, `PhotoGalleryPage`, `HomeKnowledgePage`, `RobotPage`, `SystemHealthPage` generated via `yarn rw g page` so all 6 nav links resolve; still the generator's placeholder content, no real ports yet.

**`system-health.html` scoped out of this diff, not just deferred to "later":** its log table (`/api/system-logs`) and per-service rate-stats tiles (`/api/service-stats`) hit routes that Diff 7 **intentionally dropped**, not carried forward with an equivalent — there's no DB-backed logging going forward, and a Grafana/Loki replacement is explicitly a separate future initiative. So System Health can't be a 1:1 port; when it's picked up, plan on porting only the service-health dots (`health` Function), device health table (`deviceHealthStats`), and CPU/mem gauges (`stats` Function), and dropping the log table/rate-stats tiles until that observability work lands.

**Smoke-tested end to end** against the real `redwood-db` and a real headless-Chromium browser (Playwright, driven directly since `chromium-cli` wasn't available in this sandbox) with `cf-access-authenticated-user-email: nries1@gmail.com` set as a request header (same substitute for real Cloudflare Access already used for every curl-based smoke test in this doc): Home page renders all 6 links, no console errors. Devices page renders all 4 real devices with correct badges/env-readings/tank-levels/pump-events, matching `devices.html`'s layout closely. Without the header (plain local-dev browser hit, no way to fake CF Access from an actual browser navigation — a real limitation, not a gap in this diff), the page fails gracefully: GraphQL returns `UNAUTHENTICATED`, the page shows "Failed to load devices." rather than crashing, and the admin buttons stay hidden. **Add Device modal**: created a real device (`ZZTEST01`), confirmed it appeared in the grid immediately (via the mutation's `refetch()`), then deleted it via a direct `deleteDevice` mutation afterward. **Device Settings modal**: changed the Living Room Water Tower's tank capacity from the default (30, unset) to 25 through the real UI, confirmed the change rendered, then restored the original empty config via `updateDeviceConfig(..., { tank_capacity_gallons: null })`. Verified via a final `devicesWithStatus` query that all 4 devices' configs matched their pre-test state exactly. The 5 stub pages and a `/nonexistent` route were all hit for console/network errors — none found.

**Not yet done:** the 5 stub pages (Plant Care, Photo Gallery, Home Knowledge, Robot, System Health) are placeholder-only — see the roadmap this diff's plan laid out for their sequencing and the open questions specific to each (chart-library choice for Plant Care, vision-people-enrollment scope for Home Knowledge, the MQTT→SSE architecture change for Home/Robot).

### Diff 9 — Real Home page (robot head) + a production `apiUrl` bug found by actually testing the deployed container (2026-07-12)

Diff 8's Home page was deliberately a placeholder nav hub; `index.html`'s animated robot head/speech bubble/live vision tracking was explicitly deferred pending the MQTT→SSE decision, which Diff 6 already resolved. This diff does the real port.

**`RobotHead`** (`web/src/components/RobotHead/RobotHead.tsx`) — the SVG head as a controlled component (`pan`, `tilt`, `mouthWidth`, `antennaColor` props), factored out since it's explicitly shared with `robot.html`'s not-yet-ported page too, per Diff 8's own note.

**`useVisionStream`** (`web/src/hooks/useVisionStream.ts`) — `EventSource` against `/vision/stream`, replacing the old page's direct browser→MQTT-over-websocket connection (`ws://<host>:9001`), per the Diff 6 architecture decision (that broker allows anonymous read/write to every topic, including the pump command topic; `/vision/stream` is read-only and rides the same CF-Access auth as everything else).

**Real backend gap found, not just a frontend port:** `/vision/stream` (Diff 6) only ever forwarded `robot/vision/result` (the slow CNN recognition topic) to SSE clients — `robot/vision/tracking` (the fast Haar-cascade pan/tilt feed the old page actually used for smooth low-latency head-follow) was received but only used to bump `serviceStatus.lastPublisherFrame`, never broadcast. Fixed in `api/src/lib/mqtt.ts`: both topics now go through a `broadcastVision(type, data)` helper that wraps the payload with a `type: 'tracking' | 'result'` discriminator (the raw MQTT payloads have no topic field of their own, and now two different shapes share one SSE stream). Zero real consumers existed before this diff (confirmed in Diff 6), so changing the wire format was safe.

**`HomePage`** rewritten to match `index.html`: speech bubble (real `latestAiSummary` GraphQL query, falling back to `useConfig`'s `aiPlaceholder` while loading/absent — same ordering as the old `loadAiSummary()`), the head flanked by two nav bubbles (Photo Gallery, Private AI→`/robot`) with four more below (Plant Care, Devices, System Health, Papu's Memory), each carrying the old page's per-link `pan`/`tilt` hover values. Ported the old vanilla-JS state machine as-is: hover overrides live tracking (`navHoverActive` ref), losing tracked faces re-centers after 800ms, losing hover re-centers after 400ms, and a recognized face triggers a name-cooldown'd (2 min) greeting bubble that reverts to the summary after 5s.

**Real, pre-existing production bug found — not introduced by this diff, but only surfaced now:** this is the first time a page was smoke-tested against the actual built docker image via a real browser (Playwright against `http://localhost` with the CF-Access header spoofed), rather than `yarn rw dev` or bare curl. `redwood.toml`'s `[web] apiUrl` was still the stock `"/.redwood/functions"`, which assumes something in front rewrites that prefix to the api process (Netlify's rewrite rule, or `yarn rw serve`'s default two-process web+api split with a reverse proxy in front of both). Neither applies here — this app runs one custom Fastify process (`api/src/server.ts`) that mounts graphql/functions at bare paths with no prefix. Effect: every GraphQL query 404'd outright (POST isn't covered by `server.ts`'s `onRequest` SPA-fallback hook, which only intercepts GET, so it fell through to Redwood's own routing and hit nothing), and every plain Function call (`config`, `adminStatus`, etc.) silently served `index.html` back with a 200 (GET is covered by the fallback hook; `/.redwood/functions/config` isn't in its `apiPaths` allowlist, so it looked like a client-route request and got the SPA shell) — swallowed by `apiFetch`'s catch, so it looked like "config just isn't set" rather than an outright failure. **This affected every already-shipped page, including Devices from Diff 8** — that page's own browser smoke-test evidently ran under `yarn rw dev` (which has a working dev-mode proxy for that prefix), not the built image, so it never caught this.
- First fix attempt (`apiUrl = "/"`) was itself wrong and caught by the same browser test: `apiFetch`'s `` `${RWJS_API_URL}${path}` `` with both sides starting with `/` produces `"//config"`, which browsers parse as a protocol-relative URL — `config` becomes a **hostname**, not a path, giving `net::ERR_NAME_NOT_RESOLVED`. Landed on `apiUrl = ""` instead, verified by grepping the actual built bundle inside the container for the baked-in `RWJS_API_URL` value, not just re-reading the source.
- **Docker layer caching genuinely bit here, independent of the config bug itself:** the first `apiUrl = ""` rebuild via plain `docker compose build redwood` produced a container still serving the *previous* (`"/"`) bundle — confirmed by grepping the built JS inside the running container, not by assumption. A `--no-cache` rebuild fixed it (visibly reinstalling all 1697 yarn packages from scratch that the cached build had skipped). Root cause not fully diagnosed (BuildKit cache import behaving unexpectedly across two back-to-back builds of the same Dockerfile with only `redwood.toml` changed), but worth knowing this can happen again: **verify the deployed bundle's actual baked-in config directly (`docker exec ... grep` the built JS) after a config-only change, don't trust a clean `docker compose build` exit code alone.**

**Smoke-tested end to end** against the real deployed container (`docker compose build redwood` + `up -d redwood`, not `yarn rw dev`) via Playwright/headless Chromium with the CF-Access header spoofed (same substitute used throughout this doc): `/config`, `/graphql`, `/vision/stream` all confirmed 200 with correct content-types and bodies (verified via `page.on('response')`, not just the page rendering without errors). Screenshotted the rendered page (head, eyes, mouth, antenna, speech bubble with a real live AI summary, all 6 nav bubbles correctly colored). Verified the pose mechanism directly via `getComputedStyle(svg).transform`, not just visually: idle is the identity matrix, hovering a nav bubble produces the expected `matrix3d` for that bubble's `pan`/`tilt`, and it returns to identity after mouse-leave. **Not exercised:** the live tracking/recognition path itself — `mqtt-broker` isn't running in this environment (same standing limitation noted since Diff 4), so no real MQTT messages exist to drive `onmessage` end-to-end; the hover-pose and greeting-cooldown logic were verified via direct DOM/timer inspection instead, and the SSE plumbing was verified up to "connects, authenticates, receives the keepalive comment" (matching Diff 6's own testing boundary).

# Known Issues

## Prisma + pgvector (`home_knowledge.embedding`)
The old db has one column that doesn't map cleanly to Prisma:

```sql
embedding vector(768)
-- with a partial HNSW index:
CREATE INDEX idx_home_knowledge_embedding ON home_knowledge
  USING hnsw (embedding vector_cosine_ops) WHERE (embedding IS NOT NULL);
```

Prisma has no native `vector` type and no way to declare an HNSW/partial index in schema.prisma. Workaround used:

- `datasource db` declares `extensions = [vector]` (requires the `postgresqlExtensions` preview feature on the generator) so Prisma emits `CREATE EXTENSION IF NOT EXISTS "vector";` in the migration automatically.
- The column itself is declared as `embedding Unsupported("vector(768)")?` on the `HomeKnowledge` model. Prisma will create/alter the column correctly, but the generated Prisma Client has **no typed accessor** for it — any read/write of `embedding` has to go through `$queryRaw`/`$executeRaw`.
- The HNSW index can't be expressed via `@@index` at all (no access-method or partial-index support in Prisma schema syntax), so after running `yarn rw prisma migrate dev --create-only`, the generated `migration.sql` needs a manual line added before applying:
  ```sql
  CREATE INDEX "idx_home_knowledge_embedding" ON "home_knowledge"
    USING hnsw ("embedding" vector_cosine_ops) WHERE ("embedding" IS NOT NULL);
  ```
- This is a one-time manual step per migration that touches this table/index — if `schema.prisma` changes again in a way that regenerates `home_knowledge`'s DDL (e.g. `prisma migrate dev` without `--create-only`, or `db push`), the HNSW index has to be re-added by hand again, since Prisma's migration diffing doesn't know about it.

### Upstream status (checked July 2026)

No native fix coming soon for the ORM version we're on — this workaround is the correct long-term approach, not a stopgap for a release that's about to land.

- **Stable Prisma ORM (v6.x/7.x):** no native vector type. [ORM 6.13.0](https://www.prisma.io/blog/orm-6-13-0-ci-cd-workflows-and-pgvector-for-prisma-postgres) added pgvector as an *enabled extension* for Prisma Postgres, but reading/writing vector columns still requires the same `Unsupported()` + custom migration + raw/TypedSQL approach used here. The post says native support is "coming soon" with no date attached.
- **Open tracking issues, nothing committed:**
  - [prisma/prisma#26546 "First class Vector support"](https://github.com/prisma/prisma/issues/26546) (Mar 2025) — open, no linked PR or milestone.
  - [prisma/prisma#18442 "Support for pg_vector"](https://github.com/prisma/prisma/issues/18442) (Mar 2023) — open, no assignee.
  - [prisma/prisma#28867](https://github.com/prisma/prisma/issues/28867) — a regression where Prisma 7.1.0 misdetects schema drift on `Unsupported("vector")` columns. Closed as a bug but no confirmed fix has landed — worth checking before upgrading the Prisma version pinned in this repo.
- **Where real work is happening:** not core Prisma ORM, but **Prisma Next** (the experimental successor, eventually Prisma 8), via its extension model. `@prisma-next/extension-pgvector` provides typed vector columns (`field.column(vector(1536))`) and a `cosineDistance()` query method compiling to the pgvector `<=>` operator. Early Access only — not something to build on yet, but worth revisiting once it graduates out of Early Access.
