# Issues
1. ~~Sensor health chart displays nothing at https://papushome.us/plant-care~~ **DONE** — the chart wasn't broken, it was starved: `tankReadingDailyStdDev` grouped readings by calendar day, but the tank firmware only publishes on a ≥1.5% level change (see Follow-ups #1), so most 7-day windows only ever have 0-1 distinct days with data and the chart never had the ≥2 points it needed to render. Replaced day-bucketing with a per-reading rolling `STDDEV_POP` (trailing 5-reading window) — renamed `tankReadingDailyStdDev` → `tankReadingRollingStdDev` (`api/src/graphql/tankSensorMetrics.sdl.ts`, `api/src/services/tankSensorMetrics/get.ts`, `web/src/components/SensorHealthCell/SensorHealthCell.tsx`). Verified against live data via direct SQL and an authenticated GraphQL call. Paired with the firmware heartbeat fix in Follow-ups #1 (now flashed to the Living Room tower) so the chart gets a steady trickle of points even when the tank is idle.
2. ~~logs are noisy and it's not clear which service corresponds to which actual data. When I visit http://100.122.11.16:3030/d/logs-overview/logs-overview?orgId=1&refresh=30s&var-service=redwood I see grafana, loki, ollama, promtail, redwood, and redwood-db. It's hard to know which logs relate to graphql, mqtt, hardware, or the db~~ **DONE** — the `module` label/dashboard variable mechanism already existed (`api/src/lib/logger.ts`'s `moduleLogger()`, promtail's `json`+`labels` pipeline stages, the dashboard's `$module` var querying `label_values(module)`) and already covered `graphql`/`mqtt`/`chat` — the actual gap was coverage, not missing infrastructure. Fixed:
   - `db.ts`'s Prisma query/warn/error logging was on the bare untagged `logger` — now tagged `db`, directly closing the exact gap named in the complaint.
   - `mqtt.ts` only had one `mqtt` tag covering both broker transport (connect/reconnect/subscribe/error) *and* device data ingestion (tank/environment readings, watering commands, device presence) — split into `mqtt` (transport) and a new `hardware` tag (device data), so "is the broker connection flaky" and "is a specific sensor misbehaving" are now separately filterable, matching the other category the complaint named by name.
   - Five more files were on the bare untagged `logger` with no module value at all (`aiSummaryService.ts`, `canWater/get.ts`, `displayStats.ts`, `weather.ts`, `stats.ts`) — tagged `chat`, `hardware`, `hardware`, `api`, `api` respectively (the latter two are misc Fastify utility functions that aren't really graphql/mqtt/chat/hardware/db, so a small `api` catch-all fits better than forcing them into an inaccurate bucket).
   - No dashboard JSON changes needed — the `$module` variable is already `label_values(module)`, a live query, not a hardcoded list.
   - Verified live: rebuilt+restarted `redwood`, confirmed via `docker compose exec loki wget -qO- http://localhost:3100/loki/api/v1/label/module/values` that `chat`/`db`/`graphql`/`mqtt` are all real indexed Loki labels post-restart (`db` in particular is new, proving the fix). `hardware`/`api` use the identical mechanism, just haven't fired yet (they're mostly on failure paths or infrequent events) — not a gap, same graceful-absence behavior as any other module before its first log line.
3. ~~nginx.conf is still in the repo, but I think that is unused now right?~~ **DONE** — nginx.conf has been removed from the repo.
4. ~~.env.example, and .env.defaults don't have any of the variables from .env that are needed to run the app~~ **DONE** — `.env.example` now documents every var in `.env`, including a "Legacy / not read by the app" section for the ones that turned out to be dead (`PORT`, `ZIP_CODE`, `CLOUDFLARE_TEAM_NAME`, `WYZE_API_KEY`, `WYZE_KEY_ID`).
5. ~~We've experimented with several models and landed on qwen2.5:7b because it offered the best balance of speed and accuracy. Llama 3.2 just din't follow prompt instructions closely enough. Gemma4 and qwen 3 were too big for my gpu.~~ **DONE** — written up in `SETUP.md`'s "Choosing an LLM" section; `OLLAMA_MODEL=qwen2.5:7b` is the documented default in `.env.example`.
6. ~~The webpage title just says "Redwood" (browser tab / bookmarks) — `web/src/index.html` has no `<title>` tag at all, so it falls back to the framework default. Favicon (`papu.ico`/`papu.png`) is already branded, just the title isn't.~~ **DONE** — root cause was actually `redwood.toml`'s `[web] title = "Redwood App"` (the `%AppTitle` RedwoodProvider's `titleTemplate="%PageTitle | %AppTitle"` pulls from), not `index.html`. Changed to `"Papu's Home"`. Needs a rebuild+redeploy to take effect in production.
7. Booking the 12:45pm tabata class today (2026-07-14) failed. **Partially fixed — see caveat below, real automated purchase may just not work reliably.** Root causes found and fixed, in the order discovered:
   - `web-agents` service wasn't running at all — same shape of bug as the Wyze/Home Assistant issue in Follow-ups #2. Started it (`docker compose up -d web-agents`, real MINDBODY_USERNAME/PASSWORD already present in `.env`).
   - `bookTabata.ts`'s classic-scheduler `studioid=275991` is dead — confirmed live, shows an empty week every day. Real classes only exist on MindBody's newer consumer "Explore" listing (`mindbodyonline.com/explore/locations/mike-montanez-tabata-ultimate-fitness-prospect-heights`), which the code never talked to. On top of that, today's 12:30pm class is actually named "HITT Xpress", not "Tabata" (real "Tabata"-named classes exist today too, just at 4/5:15/6:30/7:30pm), so a plain name filter would never have matched it even with the right studio ID.
   - Fix: added an Explore-listing fallback (`web-agents/src/mindbody/exploreBooking.ts`), used when the classic flow fails, wired in for Tabata via a new `overrideUrl` param on `bookMindbodyClass`. Extracted matching to a shared `selectClassEntry()` (`matchClass.ts`): match name+time first, and if nothing matches by name, fall back to whatever's at the requested time — the "Tabata" vs "HITT Xpress" case. Extracted MindBody login-filling into shared `signInMindbody()` (`auth.ts`).
   - **Then it actually shipped a false positive in production.** Once deployed, asking papu to book the class returned *"I have reserved the 12:30 PM Tabata class for you... $20.00"* — but nothing was booked (confirmed against MindBody's own account/schedule page: *"Looks like you haven't booked any classes yet"*). Root cause: after a real (unblocked) "Buy" click, MindBody can genuinely reject the order server-side — reproduced live and consistently: *"Error — We're unable to complete your order at this time. Please try again."* Most likely cause: reCAPTCHA Enterprise (confirmed active on this checkout via network trace) scoring the headless Playwright session as suspicious. The code only waited a fixed 2s after clicking Buy and fell back to an ambiguous `"Booked (unconfirmed)"` success message whenever its confirmation regex didn't match — so it never even looked for this error text, and reported success on a rejection.
   - Fixed the detection, not (yet, maybe not possible) the underlying rejection: `exploreBooking.ts` now waits for the "PURCHASING..." pending state to actually resolve, explicitly recognizes MindBody's real rejection text and throws it verbatim, and otherwise throws rather than ever guessing "booked" without unambiguous confirmation — a false failure is recoverable (retry), a false "you're booked" isn't (you find out by not having a reservation when you show up). Applied the identical fix to the classic scheduler's equivalent ambiguous fallback in `bookClass.ts` for consistency, since it has the same latent risk.
   - Two more real bugs found and fixed while re-verifying this: (a) a TrustArc cookie-consent overlay (`#consent_blackbar`/`#trustarc-banner-overlay`) that loads asynchronously and intercepts clicks — now removed defensively via JS before every click in the flow, rather than trying to click its own "Ok" button (same race). (b) A genuine wrong-date bug: after the login redirect, the Explore listing silently resets to *today's* date, and the code didn't re-select it — for a test booking on 2026-07-15, this silently matched and would have booked a same-named/same-time class on 2026-07-14 instead, with no error at all. Fixed by re-selecting (and, this time, actually verifying via the "Classes on {date}" heading) the date after login too.
   - **Open caveat, not resolved:** the reCAPTCHA-driven server-side rejection reproduced 3/3 times on real (unblocked) purchase attempts against different classes/dates during verification. It's unclear whether real automated purchases through this flow can succeed at all — this fix guarantees papu will never lie about a booking succeeding, but doesn't guarantee bookings will actually go through. Today's real class was booked manually instead.
   - Verified throughout: unit-tested `selectClassEntry`'s 4 branches directly; live-verified the corrected date-selection and name/time-fallback against several real dates/classes; live-verified (with the finalize-payment call network-blocked, to avoid spending a real pass credit while iterating) that the flow reaches a real MindBody checkout page; live-verified (this time genuinely unblocked, against a different day's class to avoid touching today's already-completed manual booking) that a real purchase attempt reproduces the honest rejection end to end with no false "Booked" claim.
   - **Follow-up hardening pass**, aimed at the reCAPTCHA/bot-risk rejection itself (still can't confirm it fixes the rejection rate — see caveat above, unchanged):
     - **Humanized interactions** (`web-agents/src/mindbody/humanInteraction.ts`) — `humanClick`/`humanType`/`humanIdleScroll` replace instant, zero-travel Playwright clicks and one-shot `.fill()` with randomized mouse-travel paths, per-keystroke delay, and idle scrolling, wired into both the login form (`auth.ts`) and the booking flows. Found and fixed a real bug this introduced: `humanClick` didn't scroll the target into view first (Playwright's own `.click()` does this implicitly), so a class further down the list than the first got a `boundingBox()` outside the actual viewport and the click silently landed nowhere — the whole flow just stalled on the same page with no error. Confirmed live before/after with the same class/date. General lesson from this whole pass: hit the live site enough times in one session (dozens, today) and it gets hard to tell a real regression apart from the site's own rate-limiting/reputation response to repeated automated traffic from one IP — worth spacing out future live verification rather than iterating rapid-fire.
     - **Session persistence** — new `WebAgentSession` table (`api/db/schema.prisma`, migration `20260714201735_add_web_agent_sessions`) storing serialized Playwright `storageState` (cookies + localStorage) keyed by profile (`"mindbody"`, shared across studios since it's one real account). `web-agents/src/db.ts` + `sessionStore.ts` talk to it directly via a plain `pg` client (deliberately not Prisma Client — web-agents is a separate, independently-deployed service outside the yarn workspace, and pulling in Prisma's codegen + native engine binaries for one table was more machinery than the job needed). `bookMindbodyClass` loads it before creating the browser context and saves it back in a `finally` block regardless of outcome — confirmed live: a real row with 31 cookies got persisted even from a run that ultimately failed.
     - **Headed browser + noVNC** — `web-agents/Dockerfile` now installs `xvfb`/`x11vnc`/`novnc`/`websockify`; `start.sh` boots Xvfb on `:99`, x11vnc against it, and websockify serving noVNC on port 6080, then execs the node app with `DISPLAY=:99` set. `chromium.launch({ headless: false, ... })` (env-var escape hatch: `WEB_AGENT_HEADLESS=true` reverts to headless). Port 6080 published the same Tailscale-only-in-practice way Grafana's 3030 already is. Verified for real: captured the actual X11 framebuffer mid-run (not a Playwright screenshot — the real shared display x11vnc serves) and got a genuine visible Chrome window with browser chrome, confirming noVNC will show the exact same thing live at `http://<tailscale-ip>:6080/vnc.html`.
     - Confirmed with the user: the originating IP isn't a datacenter address, since this automation always runs from the home server on the user's own residential connection — same IP their manual bookings would come from anyway, so that's not a contributing factor the way a cloud/VPS-hosted version of this would be.
8. ~~Error messages from papu robot are unhelpful. For operations that use tooling, we should surface meaningful errors for papu to relay to the user. E.g. when booking a class, let the user know why a booking failed (no classes matching the time or class name, no classes available in the account).~~ **DONE** — booking (#7), calendar writes, and HA light control already had their own specific try/catch returning honest failure text; the real remaining gap was `search_knowledge`, `water_plants`, and `update_knowledge` in `chatContext.ts`'s `executeTool`, which had *no* error handling at all. Confirmed this wasn't hypothetical: any throw from those (DB down, Ollama embedding endpoint down, a bad/nonexistent record) propagated all the way out of `runChatTurn` through the `sendChatMessage` resolver uncaught — the GraphQL mutation would 500 and the user's turn would be silently dropped (no assistant reply ever gets appended to chat history, since the throw happens before that line). Fixed by wrapping the whole tool dispatch in one outer `executeTool` that delegates to the switch (now `executeToolInner`) inside a try/catch, logging the real error under the `chat` module tag and returning `Tool "<name>" failed: <message>` as the tool result — so the LLM sees a real, specific failure and relays it honestly (the existing system-prompt rule against claiming unconfirmed success already covers this), instead of the request crashing outright. Verified live: rebuilt+restarted `redwood`, sent a real `sendChatMessage` asking it to delete a nonexistent knowledge-fact ID, confirmed in the logs that the genuine Prisma `P2025` error was thrown, caught, and logged, and that the mutation returned a normal 200 with reply *"I couldn't find the fact with ID 999999999 to delete it..."* instead of crashing. Cleaned up the test session/messages afterward.

# Test plan: verify web-agents booking reliability (do this in a fresh, separate session)

Today's hardening pass (session persistence, humanized interactions, headed browser + noVNC — see Issue #7) couldn't be cleanly verified against the actual reCAPTCHA rejection rate: dozens of live hits against MindBody in one sitting made it impossible to tell a real fix from the site's own rate-limiting/reputation response kicking in. Do this instead:

1. **Space it out.** One or two real purchase attempts, hours or a day apart from today's testing and from each other — not a rapid-fire loop. Every attempt is a real reCAPTCHA scoring event against the real site.
2. **Watch it live first.** Open `http://<tailscale-ip>:6080/vnc.html` before triggering a booking, so you see the browser navigate/login/click in real time instead of only reading the final result.
3. **Consider warming the session by hand.** Through the noVNC-visible browser, manually click through a real MindBody login once yourself before the first automated attempt, so the persisted session (`web_agent_sessions` table, profile `"mindbody"`) carries genuine human-driven login history rather than only ever having been logged in by automation.
4. **Pick a throwaway class/date** — something you don't actually need, far enough out that a real success doesn't create an unwanted commitment, still covered by the existing pass (so a real success costs nothing).
5. **Trigger it** — either ask papu to book it through chat (exercises the full path, including what the LLM actually relays), or hit the endpoint directly:
   ```bash
   docker run --rm --network papushome_default curlimages/curl -s -X POST http://web-agents:3001/book-tabata \
     -H "Content-Type: application/json" \
     -d '{"date":"<iso-date>","className":"Tabata","preferredTime":"<time>"}'
   ```
6. **Check ground truth independently, every time** — regardless of what the tool reports, confirm via MindBody's own account page (`https://www.mindbodyonline.com/explore/account/schedule`) whether a reservation actually exists. Don't just trust the returned message — that's the whole point of this exercise.
7. **Confirm session persistence is actually being used**:
   ```bash
   set -a && source .env && set +a && docker compose exec -T redwood-db psql -U "$REDWOOD_POSTGRES_USER" -d "$REDWOOD_POSTGRES_DB" \
     -c "SELECT profile, updated_at, jsonb_array_length(storage_state->'cookies') AS n_cookies FROM web_agent_sessions;"
   ```
8. **If it still fails every time** with the same "We're unable to complete your order..." rejection, that's a real signal the hardening alone isn't enough. Honest next options at that point: (a) accept automated purchases don't work reliably here and keep the safe-failure behavior (already solid — it will never lie about success) as the permanent state, or (b) go further — browser fingerprint diversity, solving an interactive challenge once per profile instead of relying on invisible scoring, etc. — meaningfully bigger scope, worth a real discussion before starting rather than assuming it's wanted.


# Follow-ups
1. ~~Water tower firmware only publishes a `tank_reading` when the level changes ≥1.5% since the last report (`hardware/plant-node/src/sensor.h`, `tankMonitorTask()`). A healthy-but-idle tank (not being watered/drained) can go silent for weeks even though the sensor/WiFi/MQTT are all working fine — confirmed live on the Living Room tower (21+ day uptime, sensor reading correctly, just never crossing the threshold). This makes "idle" indistinguishable from "actually offline" in the Sensor Health chart. Consider having the device publish a periodic heartbeat reading (e.g. every Nth read regardless of change) so the dashboard can tell the difference.~~ **DONE (pending flash)** — `sensor.h` now forces a publish every `heartbeatEveryNReads` (180 reads, ~6 hours) even with no threshold-crossing change. Source change only; not yet flashed to physical towers.
2. Wyze bulb control (via the `wyzeapi` Home Assistant custom_component) was fully broken, from asking "why can't papu control my Wyze bulbs" down to four stacked bugs, all now fixed:
   - `homeassistant` service wasn't running at all — started it (no code change, just wasn't up).
   - `wyzeapi` crashed on import: cffi 2.1.0 vs. the image's compiled `_cffi_backend` 2.0.0 — a known upstream Home Assistant bug introduced in 2026.7.1, fixed in 2026.7.2. Fixed by pinning `docker-compose.yml`'s `homeassistant` image to `2026.7.2` instead of the floating `:stable` tag.
   - Even after that, Wyze cloud login failed with `SSLCertVerificationError` — this image's Alpine `ca-certificates` bundle is missing the classic "DigiCert Global Root CA" (only newer G2/G3 variants are present), which is what Wyze's cert chain terminates at. Fixed by fetching the real root cert from DigiCert's own `caIssuers` URL, verifying it's genuinely self-signed, and wiring a combined bundle (`homeassistant/ca-bundle.pem`, checked into the repo) into the container via `docker-compose.yml` (overrides the system cert path + `SSL_CERT_FILE`/`REQUESTS_CA_BUNDLE`).
   - Real bug, not hardware/env: `homeActions.ts`'s `controlLight()` treated any 200 OK from Home Assistant's `/api/services/light/*` as success, without checking whether an entity actually matched. HA doesn't error on an unknown `entity_id` — it silently no-ops with an empty response array. This let the chat assistant hallucinate `light.couch_lights` (real ones are `light.couch_1`/`light.couch_2`) and confidently report "Done, the couch lights are now on" while nothing happened. Fixed to check HA's response for real affected entities; verified the existing tool-calling loop then self-corrects on an honest failure (calls `list_lights`, retries with real IDs) with no prompt changes needed.

# Hardware CLI reference (compile / flash / monitor)

Scripts live in `scripts/`; there are no `npm run hw:*` shortcuts wired up (also noted in `SETUP.md`). All commands below are run from the repo root.

```bash
# Compile only, don't flash (sanity-check a firmware change before touching real hardware)
~/.platformio/penv/bin/pio run -d hardware/plant-node -e usb   # or -e ota
~/.platformio/penv/bin/pio run -d hardware/plant-node -e ota --target checkprogsize  # + flash/RAM usage

# List connected USB boards (to grab a --port, or just confirm detection)
arduino-cli board list

# Full flash flow over USB: upload, log the release to hardware_releases, then open the serial monitor
node scripts/device_flash.js --project plant-node --fqbn esp32:esp32:adafruit_metro_esp32s3 --model "Adafruit Metro ESP32-S3"
node scripts/device_flash.js --project environment-sensor --fqbn esp32:esp32:nologo_esp32c3_super_mini --model "Nologo ESP32-C3 Super Mini"
node scripts/device_flash.js --project rgb-display --fqbn esp32:esp32:esp32s3 --model "RGB Display ESP32-S3"

# Upload over the network (OTA/espota) instead of USB — needs avahi-daemon on Linux
# to resolve <name>.local. --name/--room re-flash the device's stored identity too
# (NVS-persisted), so always pass BOTH for a device that's already deployed, or you'll
# silently reset it to the firmware's compiled-in defaults (office_tower / office).
node scripts/flash.js --project plant-node --fqbn esp32:esp32:adafruit_metro_esp32s3 --target upload --transport ota --name living_room_tower --room living_room

# Monitor serial output without flashing (USB or OTA)
node scripts/flash.js --project plant-node --fqbn esp32:esp32:adafruit_metro_esp32s3 --target monitor
node scripts/flash.js --project plant-node --fqbn esp32:esp32:adafruit_metro_esp32s3 --target monitor --transport ota --name living_room_tower

# Regenerate hardware/lib/shared/shared_constants.h after editing shared/plant_config.json
# (currently broken — see Code smells below)
node scripts/gen_hardware_config.js
```

Used the OTA upload command above for real to flash the Sensor Health heartbeat fix (Issue #1 / Follow-ups #1) to the Living Room tower — confirmed via `avahi-resolve -n living_room_tower.local`, a clean `pio run --target checkprogsize` compile first, then a successful `espota` upload, then a fresh `tank_readings` row landing under the same `device_id` (5865D0) right after reboot.

# Code smells / anti-patterns
1. ~~16 bare `catch {}` blocks across `mqtt.ts`, `calendar.ts`, `homeKnowledgeOnboarding.ts`, `homeActions.ts`, `chatContext.ts`, `photos/get.ts`, `tts.ts`, `media.ts` — the exception isn't even captured, so nothing can be logged even if someone wanted to. Confirmed real impact, not hypothetical: `getRoomIdForDevice` returns `null` on a DB error, indistinguishable from "this device genuinely has no room"; `photos/get.ts` returns `[]` on any failure (including permission/disk errors), indistinguishable from "no photos exist." None of these failures will ever show up in the Grafana/Loki logs, since nothing is ever logged.~~ **DONE** — all catch blocks now capture and log the error (commit `377d9fb`).
2. `chatContext.ts` is a 603-line god-file with five distinct responsibilities bundled together: tool schema definitions, tool dispatch (`executeTool`, a ~140-line switch), system-prompt construction, chat-turn orchestration (`runChatTurn`), and background session summarization. Worth splitting for maintainability, especially since this is the file most likely to intimidate an outside contributor's first PR.
3. Inconsistent error-handling conventions (same root cause as #1) — different functions signal failure differently with no consistent convention across the codebase: `null` (`getRoomIdForDevice`), empty array (`photos/get.ts`), a user-facing string (`calendar.ts`), `{success, summary}` (`homeActions.ts`). A contributor has to read each function individually to know how it fails.
4. Minor: 3 `any` casts in `ollama.ts`'s error-logging block (`(err as any).response?.status` etc.) — low severity, isolated to one diagnostic spot.
5. ~~`scripts/gen_hardware_config.js` writes to `hardware/shared/shared_constants.h`, but that directory doesn't exist — the real file the firmware actually includes lives at `hardware/lib/shared/shared_constants.h` (matches `lib_extra_dirs = ../lib` in `platformio.ini`). Confirmed by running it: `ENOENT: no such file or directory, open '.../hardware/shared/shared_constants.h'`, crashing instead of updating the file that matters. Anyone editing `shared/plant_config.json` and trying to regenerate the header currently can't.~~ **DONE** — fixed the output path. Also found and fixed a second latent bug while verifying: the header template never emitted `SHARED_TOPIC_DEVICE_BOOT`, even though `plant_config.json` defines `device_boot_topic` and the existing committed header has that line — running the (path-)fixed generator as-is would have silently deleted that constant on first use. Added it to the template. Verified live: ran the corrected script and diffed its output against the previously-committed header — byte-for-byte identical, confirming both the path and the content are now right.
6. ~~`scripts/log_release.js` (invoked by every `device_flash.js` run) shells out to `docker exec home-db-1 psql -U user -d plants ...` — `home-db-1` and those credentials are the legacy pre-migration stack (see `redwoodmigration.md`); the actual container is `papushome-redwood-db-1` with `REDWOOD_POSTGRES_*` creds. Confirmed no `home-db-1` container exists (`docker ps`). Every hardware flash has been silently failing to record a `hardware_releases` row — masked by the script's intentionally best-effort `catch` (warns, exits 0) so uploads still succeed, but release history for every flash to date is just missing.~~ **DONE** — now targets `papushome-redwood-db-1` and reads `REDWOOD_POSTGRES_USER`/`REDWOOD_POSTGRES_DB` from `.env` (a tiny inline parser, not a new `dotenv` dependency, since real env vars still take precedence and this is a standalone script outside the app's own env loading). Verified live end-to-end: ran `node scripts/log_release.js --version "v0.0.test-verify" --model "Test Model"`, confirmed a real row landed in `hardware_releases` via direct `psql`, then deleted that test row.

# Nice to have for marketing this as an open source project
1. Add more services to web-agents (book flights, buy from amazon, play and suggest music, etc.)
2. Working physical robot
3. High quality demo video
4. Onboarding UI flow for the robot (interview questions, model selection, service selection (e.g. mind-body, wyze, etc.))
5. Creating npm packages for the parts of the repo that are genuinely shipable as packages. (a) api, mqtt, and microcontroller networking. (b) Home robot code to let anyone install an npm package and launch and configure their own home robot
6. Media storage optimizations and thumbnbails for scalability in displaying on the frontend.
7. `web-agents` (browser automation for booking systems like MindBody — see Issues #7 for the depth of what's actually involved: studio-ID/class-name quirks, session persistence, human-like interaction, headed-browser + noVNC) could be its own standalone npm package. Generic "log into and book a class/reservation/appointment on service X" automation seems like it'd be broadly useful beyond this project — worth checking whether something like this already exists before building it out further.
